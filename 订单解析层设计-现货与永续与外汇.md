# 订单意图解析层设计 v1

**覆盖市场**：加密现货（CRYPTO_SPOT）、U本位永续（CRYPTO_PERP_LINEAR）、外汇 CFD（FX_CFD）

**外汇侧已确认**：经纪商 IC Markets，平台 MetaTrader 5，账户模式 Netting（净额）

**定位**：本文档定义 `kq_engine/resolution/` 的完整契约与算法，是 Router 与 Venue 之间唯一的翻译层。策略端表达"中性意图"，本层翻译成"venue 原生委托规格"。

---

## 0. 范围与边界

### 0.1 本层负责

| 职责 | 说明 |
|---|---|
| 意图解析 | signed volume / side+qty / lots → 具体开平仓语义 |
| 跨零拆单 | `current=+1, volume=-2.5` → 平多1 + 开空1.5 |
| 数量价格量化 | 按 Spec 的 stepSize / tickSize / lot_step 对齐 |
| 合规预检 | min_qty / min_notional / 可平量 / 做空能力 |
| 腿意图标注 | 每条委托标注 OPEN_LONG / CLOSE_LONG / ... |

### 0.2 本层不负责（明确划出，避免职责膨胀）

| 不做 | 归属 |
|---|---|
| 保证金 / 可用资金计算 | `portfolio/` |
| 限额、频率、敞口风控 | `risk/` |
| 符号映射（`BTC/USDT` → `BTCUSDT`） | `instruments/registry.py`，Resolver 只消费结果 |
| 限频、重试、连接管理 | `execution/venue_lane.py` |
| 网络请求与签名 | `venues/*` |
| 持仓记账 | `portfolio/state.py` |

**Resolver 是纯函数**：`(OrderRequest, PositionState, InstrumentSpec) -> Resolution`。无 I/O、无时钟、无全局状态。这是它能被穷举测试的前提。

---

## 1. 三市场特性对照

设计前先对齐事实，这决定了 Spec 字段与 Bucket 维度。

**外汇侧已确认**：经纪商 IC Markets，平台 MetaTrader 5，账户模式 **Netting（净额）**。

| 维度 | 加密现货 | U本位永续 | 外汇 CFD（IC Markets MT5） |
|---|---|---|---|
| 产品性质 | 真实币 | 永续合约 | **差价合约**（跟踪即期汇率，不交割货币） |
| 可做空 | ✗ | ✓ | ✓ |
| 数量单位 | base ccy（BTC） | base ccy（BTC） | **lot**（1 lot = 100,000 base ccy） |
| 最小单位 | stepSize 0.00001 | stepSize 0.001 | volume_step 0.01 |
| 名义额下限 | minNotional 5 USDT | minNotional 5 USDT | 无（但有 volume_min） |
| 报价精度 | tickSize 0.01 | tickSize 0.10 | point 1e-05；**pip** EURUSD 0.0001 / USDJPY 0.01 |
| 持仓模式 | 单向净持仓 | 单向（hedge 可选） | **Netting 已确认**（hedging 分支不启用） |
| 平仓标识 | 无 | `reduceOnly` | 无（netting 下经纪商自动净轧） |
| 平仓顺序 | — | — | **不适用**（IC 不接受美国客户，无 NFA FIFO 约束） |
| 持仓成本 | 无 | 资金费率（8h/4h） | **swap 隔夜融资费**（每日，周三三倍） |
| 佣金 | bps（名义额万分比） | bps | **$3.50/lot/边**（Raw MT5）或零佣金（Standard） |
| 杠杆 | 无 | 1~125x | **30x（ASIC 实体）/ 最高 500x（离岸 FSA 实体）** |
| 到期 | 即时 | 无到期 | **无到期**（CFD 特性，与现货 T+2 交割不同） |

**关键差异点**：

1. **FX 是 CFD 不是现货**。产品类型定为 `FX_CFD`，无 T+2 交割概念，swap 是经纪商融资定价而非银行间利差结算。这影响成本模型与到期处理，但不影响下单语义。

2. **FX 的 `volume` 单位是 lot 而非货币量**。`send_order("EUR/USD", volume=Decimal("1"))` = 1 标准手 = 100,000 EUR。与 crypto 的 `volume=1` = 1 BTC 相差 10 万倍，必须在 SDK 层显式警示（见 §4.3）。

3. **pip 因货币对而异**。JPY 计价的对 pip=0.01，其余 pip=0.0001。Spec 需显式存 `pip_size`，**不可从 `price_tick × 10` 推导**（4 位/2 位老式报价下 pip == price_tick，推导会错）。

4. **Netting 让 FX 与永续单向同构**。已确认账户为 Netting，因此 `_resolve_hedging` 分支（ticket 追踪、FIFO 多腿、`position_id`）**v1 不启用**，只保留代码骨架。FxResolver 主路径与永续单向几乎一致，仅差 lot 单位与 `reduce_only=None`。

5. **成本模型不能用单一 bps 表达**。IC Markets 两个平台的佣金计价单位不同（MT5 按 lot，cTrader 按名义额），且 Standard 账户成本全在点差里。需要 `FeeModel` 抽象（§2.1）。

6. **MT5 接入的两个硬约束**：
   - **仅 Windows**：官方 `MetaTrader5` Python 包只发 Windows wheel，且需 MT5 终端进程常驻并已登录
   - **同步阻塞 API**：所有 `mt5.*` 调用阻塞事件循环。7×24 实盘中一次慢查询会卡住所有品种的行情处理，**必须隔离到线程池**（见 §13.3）

7. **MT5 无幂等键**。REST 交易所有 `clientOrderId` 可去重，MT5 没有对应概念——重复提交会真的下两单。FX 侧幂等**必须在框架本地实现**（去重表 + `mt5_magic` 过滤），不能依赖 venue。这是实盘安全的关键差异（§11.3）。

---

## 2. InstrumentSpec

### 2.1 完整定义

```python
"""kq_engine/instruments/spec.py"""
from __future__ import annotations

from dataclasses import dataclass, replace
from decimal import Decimal
from enum import Enum
from typing import Optional

from kq_engine.instruments.capability import Capability, CapabilitySet


class ProductType(str, Enum):
    CRYPTO_SPOT = "CRYPTO_SPOT"
    CRYPTO_PERP_LINEAR = "CRYPTO_PERP_LINEAR"   # U本位
    CRYPTO_PERP_INVERSE = "CRYPTO_PERP_INVERSE" # 币本位（预留）
    FX_CFD = "FX_CFD"                           # 差价合约，跟踪即期汇率但不交割
    # ── Phase 2 ──
    CN_FUTURES = "CN_FUTURES"
    CN_STOCK = "CN_STOCK"
    HK_STOCK = "HK_STOCK"


class PositionMode(str, Enum):
    ONE_WAY = "ONE_WAY"     # 净持仓（crypto 交易所单向）
    HEDGE = "HEDGE"         # 双向持仓（永续 hedge mode / MT5 hedging 账户）
    NETTING = "NETTING"     # MT5 netting 账户（算法同 ONE_WAY，命名区分来源）


class FeeModelKind(str, Enum):
    """成本计价方式。IC Markets 两种平台的佣金单位不同，不能只用 bps 表达。"""
    BPS = "BPS"                       # 名义额万分比（crypto 交易所）
    PER_LOT = "PER_LOT"               # 每 lot 固定额（MT4/MT5 Raw Spread）
    PER_NOTIONAL_UNIT = "PER_NOTIONAL_UNIT"  # 每 N 单位名义额固定额（cTrader Raw）
    SPREAD_ONLY = "SPREAD_ONLY"       # 零佣金，成本全在点差（Standard 账户）


@dataclass(frozen=True)
class FeeModel:
    """单边费率。往返 = 开仓 + 平仓各一次。"""
    kind: FeeModelKind
    rate: Decimal                     # bps 值 / 每 lot 金额 / 每单位金额
    per_notional: Decimal = Decimal("1")   # PER_NOTIONAL_UNIT 的分母，如 100000
    currency: str = "USD"             # 计费币种；IC Markets 按账户基础货币转换
    spread_markup_pips: Decimal = Decimal("0")  # Standard 账户的点差加成


@dataclass(frozen=True)
class SwapModel:
    """CFD 隔夜融资费。注意周三三倍惯例（按持仓日结算的 T+2 规则遗留）。"""
    long_per_lot: Decimal             # 多头每日（quote ccy）
    short_per_lot: Decimal            # 空头每日
    triple_day: int = 2               # ISO weekday，2=周三（外汇惯例）
    swap_free: bool = False           # Islamic 账户
    holding_fee_per_lot: Decimal = Decimal("0")  # swap-free 账户改收的持有费


@dataclass(frozen=True)
class InstrumentSpec:
    """品种交易规格。回测按 bar.timestamp 查询当时生效的版本。"""

    # ── 身份 ──
    symbol: str                       # 中性符号 "BTC/USDT" / "EUR/USD"
    product: ProductType
    venue: str                        # "binance_spot" / "binance_futures" / "icmarkets_mt5"
    native_symbol: str                # "BTCUSDT" / "EURUSDm" / "USDJPYm"
    base_ccy: str
    quote_ccy: str

    # ── 数量（venue 原生单位）──
    qty_unit: str                     # "BTC" / "LOT"
    qty_step: Decimal                 # stepSize / lot_step
    qty_min: Decimal
    qty_max: Decimal
    contract_multiplier: Decimal      # 1 单位 = 多少 base ccy；spot/perp = 1，FX = 100000

    # ── 价格 ──
    price_tick: Decimal               # tickSize / point
    price_decimals: int               # 2 / 1 / 5 / 3
    pip_size: Decimal                 # FX 专用：0.0001 或 0.01；crypto 为 Decimal(0)
    min_notional: Decimal             # quote ccy 计价；FX 通常为 0

    # ── 持仓模式 ──
    position_mode: PositionMode       # 决定 Resolver 走哪个分支（不是能力）

    # ── 能力声明（§15.1，替代分散布尔字段）──
    capabilities: CapabilitySet       # frozenset[Capability]

    # ── 成本 ──
    fee: FeeModel                     # 单边费率模型
    funding_interval_hours: int       # 永续 8 或 4；其余 0
    swap: Optional[SwapModel]         # CFD 隔夜融资费；crypto/现货为 None

    # ── 杠杆 ──
    leverage_max: int                 # 现货 1；永续 125；IC Markets 30(ASIC)~500(离岸)

    # ── MT5 专用（非 MT5 venue 全为默认值）──
    mt5_filling: str = "IOC"          # MT5 filling mode：FOK / IOC / RETURN
    mt5_deviation_points: int = 20    # 市价单允许滑点（point）
    mt5_magic: int = 0                # EA 标识，用于回报归属过滤

    # ── 状态与版本 ──
    status: str                       # "TRADING" / "HALTED" / "DELISTED"
    effective_from: int               # Unix ms
    effective_to: Optional[int]       # None = 当前生效
    fetched_at: int

    # ── 派生 ──
    @property
    def is_fx(self) -> bool:
        return self.product == ProductType.FX_CFD

    @property
    def qty_is_lot(self) -> bool:
        """volume 参数是否以 lot 计（仅 FX CFD）"""
        return self.qty_unit == "LOT"

    @property
    def expires(self) -> bool:
        """CFD 与永续均无到期日；期货为 True（Phase 2）"""
        return False

    def can(self, cap: Capability) -> bool:
        """能力查询。等价于 `cap in self.capabilities`，读起来更自然。"""
        return cap in self.capabilities

    @property
    def can_short(self) -> bool:
        return self.can(Capability.SHORT)

    @property
    def reduce_only_supported(self) -> bool:
        return self.can(Capability.REDUCE_ONLY)

    def with_capabilities(self, caps: CapabilitySet) -> "InstrumentSpec":
        """返回能力集被替换的副本（sandbox/testnet override 用，见 §15.4）。

        frozen dataclass 不可原地改，必须走 replace 语义。
        """
        return replace(self, capabilities=caps)
```

> **注**：`can_short` / `reduce_only_supported` 保留为**派生属性**而非字段——
> 数据源只有 `capabilities` 一处，避免两个真相源不同步。
> Resolver 代码因此无需改动（仍写 `spec.reduce_only_supported`），
> 但新增能力时只扩 `Capability` 枚举，不动 dataclass 与 DDL。

### 2.2 三市场实例

```python
# ── 能力集（§15.1）。按 venue/产品定义一次，Spec 复用 ──
from kq_engine.instruments.capability import Capability as C

CAPS_CRYPTO_SPOT = frozenset({
    C.MARKET, C.LIMIT, C.PARTIAL_FILL,
    C.STOP_MARKET, C.STOP_LIMIT, C.POST_ONLY,
    C.IOC, C.FOK, C.GTD, C.OCO, C.ICEBERG,
    C.EDIT_ORDER, C.BATCH_ORDERS, C.CANCEL_ALL,
    C.ORDER_STREAM_PUSH, C.IDEMPOTENT_CLIENT_ID, C.TRADE_FEE_IN_RESPONSE,
    # 刻意不含 C.SHORT / C.REDUCE_ONLY / C.HEDGE_MODE
})

CAPS_CRYPTO_PERP_LINEAR = CAPS_CRYPTO_SPOT | frozenset({
    C.SHORT, C.REDUCE_ONLY, C.HEDGE_MODE, C.MULTI_ASSET_MARGIN,
})

# MT5：无推送、无幂等键（§9.5），其余能力齐备
CAPS_MT5_FX_NETTING = frozenset({
    C.MARKET, C.LIMIT, C.PARTIAL_FILL,
    C.SHORT, C.NETTING_AUTO,
    C.STOP_MARKET, C.STOP_LIMIT,
    C.IOC, C.FOK, C.CANCEL_ALL,
    C.TRADE_FEE_IN_RESPONSE,
    # 刻意不含：C.ORDER_STREAM_PUSH（只能轮询）
    #          C.IDEMPOTENT_CLIENT_ID（重发会真下两单）
    #          C.REDUCE_ONLY（netting 由经纪商自动净轧）
    #          C.HEDGE_MODE（账户已确认 Netting）
})

BTCUSDT_SPOT = InstrumentSpec(
    symbol="BTC/USDT", product=ProductType.CRYPTO_SPOT,
    venue="binance_spot", native_symbol="BTCUSDT",
    base_ccy="BTC", quote_ccy="USDT",
    qty_unit="BTC", qty_step=Decimal("0.00001"),
    qty_min=Decimal("0.00001"), qty_max=Decimal("9999"),
    contract_multiplier=Decimal("1"),
    price_tick=Decimal("0.01"), price_decimals=2, pip_size=Decimal("0"),
    min_notional=Decimal("5"),
    position_mode=PositionMode.ONE_WAY,
    capabilities=CAPS_CRYPTO_SPOT,
    fee=FeeModel(kind=FeeModelKind.BPS, rate=Decimal("10"), currency="USDT"),
    funding_interval_hours=0, swap=None,
    leverage_max=1,
    status="TRADING",
    effective_from=1735689600000, effective_to=None, fetched_at=1735689600000,
)

BTCUSDT_PERP = InstrumentSpec(
    symbol="BTC/USDT", product=ProductType.CRYPTO_PERP_LINEAR,
    venue="binance_futures", native_symbol="BTCUSDT",
    base_ccy="BTC", quote_ccy="USDT",
    qty_unit="BTC", qty_step=Decimal("0.001"),
    qty_min=Decimal("0.001"), qty_max=Decimal("1000"),
    contract_multiplier=Decimal("1"),
    price_tick=Decimal("0.10"), price_decimals=1, pip_size=Decimal("0"),
    min_notional=Decimal("5"),
    position_mode=PositionMode.ONE_WAY,
    capabilities=CAPS_CRYPTO_PERP_LINEAR,
    fee=FeeModel(kind=FeeModelKind.BPS, rate=Decimal("5"), currency="USDT"),
    funding_interval_hours=8, swap=None,
    leverage_max=125,
    status="TRADING",
    effective_from=1735689600000, effective_to=None, fetched_at=1735689600000,
)

# ── IC Markets MT5，Raw Spread 账户 ──
# native_symbol 的后缀由经纪商决定（IC Markets 常见为 "EURUSDm" 或 "EURUSD"，
# 无后缀版本可能属于 Standard 账户），接入时必须用 mt5.symbols_get() 实测确认。
EURUSD_IC_RAW = InstrumentSpec(
    symbol="EUR/USD", product=ProductType.FX_CFD,
    venue="icmarkets_mt5", native_symbol="EURUSDm",
    base_ccy="EUR", quote_ccy="USD",
    qty_unit="LOT", qty_step=Decimal("0.01"),
    qty_min=Decimal("0.01"), qty_max=Decimal("500"),
    contract_multiplier=Decimal("100000"),
    price_tick=Decimal("0.00001"), price_decimals=5,
    pip_size=Decimal("0.0001"),
    min_notional=Decimal("0"),
    position_mode=PositionMode.NETTING,
    capabilities=CAPS_MT5_FX_NETTING,
    fee=FeeModel(
        kind=FeeModelKind.PER_LOT,
        rate=Decimal("3.50"),          # 单边，往返 $7.00/lot
        currency="USD",
        spread_markup_pips=Decimal("0"),   # Raw 账户点差均值 ~0.1 pip，不加成
    ),
    funding_interval_hours=0,
    swap=SwapModel(
        long_per_lot=Decimal("-7.20"),  # ⚠️ 示例值，须用 symbol_info().swap_long 实测
        short_per_lot=Decimal("2.10"),
        triple_day=2,
    ),
    leverage_max=30,                    # ⚠️ ASIC 实体；离岸 FSA 实体可达 500，须按账户确认
    mt5_filling="IOC", mt5_deviation_points=20, mt5_magic=100001,
    status="TRADING",
    effective_from=1735689600000, effective_to=None, fetched_at=1735689600000,
)

# ── IC Markets MT5，Standard 账户（若实际是这类账户，用此变体）──
EURUSD_IC_STANDARD = InstrumentSpec(
    symbol="EUR/USD", product=ProductType.FX_CFD,
    venue="icmarkets_mt5", native_symbol="EURUSD",
    base_ccy="EUR", quote_ccy="USD",
    qty_unit="LOT", qty_step=Decimal("0.01"),
    qty_min=Decimal("0.01"), qty_max=Decimal("500"),
    contract_multiplier=Decimal("100000"),
    price_tick=Decimal("0.00001"), price_decimals=5,
    pip_size=Decimal("0.0001"),
    min_notional=Decimal("0"),
    position_mode=PositionMode.NETTING,
    capabilities=CAPS_MT5_FX_NETTING,
    fee=FeeModel(
        kind=FeeModelKind.SPREAD_ONLY,
        rate=Decimal("0"),
        currency="USD",
        spread_markup_pips=Decimal("0.8"),  # 成本全在点差，回测必须用 bid/ask 撮合
    ),
    funding_interval_hours=0,
    swap=SwapModel(long_per_lot=Decimal("-7.20"), short_per_lot=Decimal("2.10")),
    leverage_max=30,
    mt5_filling="IOC", mt5_deviation_points=20, mt5_magic=100001,
    status="TRADING",
    effective_from=1735689600000, effective_to=None, fetched_at=1735689600000,
)

USDJPY_IC = InstrumentSpec(
    symbol="USD/JPY", product=ProductType.FX_CFD,
    venue="icmarkets_mt5", native_symbol="USDJPYm",
    base_ccy="USD", quote_ccy="JPY",
    qty_unit="LOT", qty_step=Decimal("0.01"),
    qty_min=Decimal("0.01"), qty_max=Decimal("500"),
    contract_multiplier=Decimal("100000"),
    price_tick=Decimal("0.001"), price_decimals=3,
    pip_size=Decimal("0.01"),          # ← JPY 对例外，不可从 price_tick×10 推导
    min_notional=Decimal("0"),
    position_mode=PositionMode.NETTING,
    capabilities=CAPS_MT5_FX_NETTING,
    fee=FeeModel(kind=FeeModelKind.PER_LOT, rate=Decimal("3.50"), currency="USD"),
    funding_interval_hours=0,
    swap=SwapModel(long_per_lot=Decimal("3.50"), short_per_lot=Decimal("-9.80")),
    leverage_max=30,
    mt5_filling="IOC", mt5_deviation_points=20, mt5_magic=100001,
    status="TRADING",
    effective_from=1735689600000, effective_to=None, fetched_at=1735689600000,
)
```

**IC Markets MT5 实测取值方式**（Spec loader 应这样填，不要硬编码）：

```python
import MetaTrader5 as mt5

mt5.initialize()
info = mt5.symbol_info("EURUSDm")

spec_fields = {
    "qty_step":  Decimal(str(info.volume_step)),    # 0.01
    "qty_min":   Decimal(str(info.volume_min)),     # 0.01
    "qty_max":   Decimal(str(info.volume_max)),     # 500.0
    "price_tick": Decimal(str(info.point)),         # 1e-05
    "price_decimals": info.digits,                  # 5
    "contract_multiplier": Decimal(str(info.trade_contract_size)),  # 100000
    "leverage_max": account_leverage,               # mt5.account_info().leverage
    "swap_long":  Decimal(str(info.swap_long)),     # ⚠️ 单位由 swap_mode 决定，见下方易错点1
    "swap_short": Decimal(str(info.swap_short)),
    "swap_mode":  info.swap_mode,                   # ← 必须一起读，否则 swap 无法换算
    "triple_day": info.swap_rollover3days,          # 0=周一 … 6=周日，三倍 swap 日
    "filling":    info.filling_mode,                # 位掩码，决定 FOK/IOC/RETURN
}
```

⚠️ **两个易错点**：

1. `swap_long` / `swap_short` 的单位由 `swap_mode` 决定（points / base ccy / quote ccy / margin ccy / percent），**不是固定 USD/lot**。必须读 `info.swap_mode` 再换算，否则隔夜费会差好几个数量级。
2. `filling_mode` 是位掩码（1=FOK, 2=IOC, 4=RETURN），不同经纪商支持的组合不同。下市价单时填错模式会被直接拒（retcode 10030 `TRADE_RETCODE_INVALID_FILLING`）。


### 2.3 字段来源：哪些能从 API 拿，哪些拿不到

你说得对——**规格不该手工维护**。三个 venue 都有现成接口，Spec loader 应该直接读：

| Venue | 接口 | 返回内容 |
|---|---|---|
| 币安现货 | `GET /api/v3/exchangeInfo` | `filters[]`（LOT_SIZE / PRICE_FILTER / MIN_NOTIONAL） |
| 币安永续 | `GET /fapi/v1/exchangeInfo` | 同上 + `contractType` / `marginAsset` |
| OKX | `GET /api/v5/public/instruments?instType=SWAP` | `lotSz` / `tickSz` / `minSz` / `ctVal` |
| MT5 | `mt5.symbol_info(sym)` | `volume_step/min/max` / `point` / `digits` / `trade_contract_size` / `swap_*` / `filling_mode` |

现有代码已经在拉 `exchangeInfo`（[binance_source.py](<F:\KlineQuantWork\klinequant\gateway\market_sources\binance_source.py>):155），但 `_parse_exchange_info` 只取了 symbol/name/type（:161-168），**filters 全丢了**。扩展它即可，不用新写抓取逻辑。

**但有三项 API 里没有，必须另想办法：**

| 字段 | 为什么拿不到 | 处理方案 |
|---|---|---|
| `pip_size` | MT5 只给 `point`（EURUSD 1e-5、USDJPY 1e-3），pip 是市场惯例不是合约属性 | **由 quote_ccy 推导**：JPY 计价 → 0.01，其余 → 0.0001。写成纯函数 + 单测覆盖主要货币对，不查 API |
| `product` / `position_mode` | 交易所返回的是 venue 自己的分类（币安 `PERPETUAL`、MT5 `SYMBOL_CALC_MODE_FOREX`），与我们的枚举不同名 | **映射表**，每个 venue 一张，随 adapter 一起维护 |
| `fee`（佣金） | 币安 `exchangeInfo` **不含费率**；MT5 `symbol_info` 也不含账户佣金 | 单独接口：币安 `GET /sapi/v1/asset/tradeFee`（需签名），MT5 `mt5.account_info()` + 经纪商费率表。**拉不到时用保守默认值 + warning** |

`pip_size` 这项尤其要注意：如果误用 `point` 当 pip，USDJPY 的止损距离会差 **100 倍**（0.30 vs 0.003）。所以推导函数必须有单测，且 Spec 加载后要做一次自检——`pip_size / price_tick` 应为 10（JPY 对）或 10（五位报价），不是整数倍就说明推导错了。

**刷新策略**（比原方案简化）：

```
启动时     : 全量拉取，与库中最新快照比对，有变更则插入新行（effective_to 封口旧行）
每日定时   : 全量重拉一次（MT5 swap 每日变动，这是硬需求）
收到拒单时 : 若错误码属精度类（币安 -1013 / MT5 10027/10030），立即重拉该品种并告警
回测时     : 按 bar.timestamp 落在 [effective_from, effective_to) 取版本
             取不到 → 用当前值 + 显式 warning 写入 EventLog，绝不静默
```

第三条是兜底：精度变更可能发生在两次定时刷新之间，拒单触发的强制刷新能自愈，不用等下一个周期。



### 2.4 DDL（DuckDB，时间快照）

```sql
CREATE TABLE IF NOT EXISTS instrument_specs (
    spec_id              VARCHAR    NOT NULL,
    symbol               VARCHAR    NOT NULL,
    product              VARCHAR    NOT NULL,
    venue                VARCHAR    NOT NULL,
    native_symbol        VARCHAR    NOT NULL,
    base_ccy             VARCHAR    NOT NULL,
    quote_ccy            VARCHAR    NOT NULL,

    qty_unit             VARCHAR    NOT NULL,
    qty_step             DECIMAL(38,18) NOT NULL,
    qty_min              DECIMAL(38,18) NOT NULL,
    qty_max              DECIMAL(38,18) NOT NULL,
    contract_multiplier  DECIMAL(38,18) NOT NULL DEFAULT 1,

    price_tick           DECIMAL(38,18) NOT NULL,
    price_decimals       INTEGER    NOT NULL,
    pip_size             DECIMAL(38,18) NOT NULL DEFAULT 0,
    min_notional         DECIMAL(38,18) NOT NULL DEFAULT 0,

    position_mode        VARCHAR    NOT NULL,
    capabilities         VARCHAR    NOT NULL,   -- JSON array of Capability（§15.1）
                                                -- 取代 can_short / reduce_only_supported 两列

    -- 成本：FeeModel 展开
    fee_kind             VARCHAR    NOT NULL,          -- BPS/PER_LOT/PER_NOTIONAL_UNIT/SPREAD_ONLY
    fee_rate             DECIMAL(38,18) NOT NULL DEFAULT 0,
    fee_per_notional     DECIMAL(38,18) NOT NULL DEFAULT 1,
    fee_currency         VARCHAR    NOT NULL DEFAULT 'USD',
    spread_markup_pips   DECIMAL(38,18) NOT NULL DEFAULT 0,
    funding_interval_hours INTEGER  NOT NULL DEFAULT 0,

    -- 成本：SwapModel 展开（NULL = 不适用）
    swap_long_per_lot    DECIMAL(38,18),
    swap_short_per_lot   DECIMAL(38,18),
    swap_triple_day      INTEGER    DEFAULT 2,
    swap_free            BOOLEAN    NOT NULL DEFAULT FALSE,
    holding_fee_per_lot  DECIMAL(38,18) NOT NULL DEFAULT 0,

    leverage_max         INTEGER    NOT NULL DEFAULT 1,

    -- MT5 专用
    mt5_filling          VARCHAR    NOT NULL DEFAULT 'IOC',
    mt5_deviation_points INTEGER    NOT NULL DEFAULT 20,
    mt5_magic            BIGINT     NOT NULL DEFAULT 0,

    status               VARCHAR    NOT NULL,
    effective_from       BIGINT     NOT NULL,
    effective_to         BIGINT,
    fetched_at           BIGINT     NOT NULL,

    PRIMARY KEY (symbol, product, venue, effective_from)
);

CREATE INDEX IF NOT EXISTS idx_specs_lookup
    ON instrument_specs (symbol, product, venue, effective_from, effective_to);
```

**注意**：现有 `orders` 表用 `DOUBLE` 存 price/quantity（[schema.py](<F:\KlineQuantWork\klinequant\storage\schema.py>) 第 86-90 行），本表一律 `DECIMAL(38,18)`。orders/fills 表需同步改造，否则精度在落库时丢失。

规格变更的刷新策略见 §2.3。

---

## 3. PositionState 与 Bucket

### 3.1 定义

```python
"""kq_engine/portfolio/state.py"""
from __future__ import annotations

from dataclasses import dataclass, field
from decimal import Decimal
from enum import Enum
from typing import Optional


class Side(str, Enum):
    LONG = "LONG"
    SHORT = "SHORT"

    @property
    def sign(self) -> int:
        return 1 if self is Side.LONG else -1

    @property
    def opposite(self) -> "Side":
        return Side.SHORT if self is Side.LONG else Side.LONG


class Age(str, Enum):
    NA = "NA"               # crypto / FX
    TODAY = "TODAY"         # 期货平今、A股 T+1
    YESTERDAY = "YESTERDAY"


class AccountKind(str, Enum):
    NORMAL = "NORMAL"
    MARGIN = "MARGIN"       # 融资融券（Phase 2）


@dataclass(frozen=True)
class PositionBucket:
    """持仓的最小记账单元。三市场下 ticket/age/account 多为默认值。"""
    side: Side
    age: Age = Age.NA
    account: AccountKind = AccountKind.NORMAL
    ticket: str = ""        # FX hedging 持仓票号；其余为空
    opened_at: int = 0      # FIFO 排序键；netting 为 0


@dataclass
class PositionState:
    """单品种持仓。Resolver 的只读输入。"""
    symbol: str
    product: ProductType
    position_mode: PositionMode
    buckets: dict[PositionBucket, Decimal] = field(default_factory=dict)

    # ── 派生投影 ──

    @property
    def net_volume(self) -> Decimal:
        """signed 净持仓（自然单位：BTC / lot）"""
        total = Decimal("0")
        for b, q in self.buckets.items():
            total += b.side.sign * q
        return total

    def side_volume(self, side: Side) -> Decimal:
        """某方向总持仓（忽略 ticket）"""
        return sum(
            (q for b, q in self.buckets.items() if b.side is side),
            Decimal("0"),
        )

    def buckets_of(self, side: Side) -> list[tuple[PositionBucket, Decimal]]:
        """某方向的桶，按 (opened_at, ticket) 升序 —— FIFO 平仓依据"""
        items = [(b, q) for b, q in self.buckets.items()
                 if b.side is side and q > 0]
        return sorted(items, key=lambda kv: (kv[0].opened_at, kv[0].ticket))

    @property
    def available(self) -> Decimal:
        """可平量（signed 绝对值）。

        本次三市场（crypto 现货 / U本位永续 / FX CFD）均为 T+0，
        直接等于净持仓绝对值。
        Phase 2 接 A股时需扣除当日买入部分（T+1），届时按 bucket.age 过滤。
        """
        return abs(self.net_volume)

    @property
    def is_flat(self) -> bool:
        return all(q == 0 for q in self.buckets.values())
```

### 3.2 三市场的 bucket 形态

| 市场 | buckets 示例 | net_volume | 说明 |
|---|---|---|---|
| 现货 | `{(LONG,NA,NORMAL,"",0): 1.5}` | +1.5 | 只有 LONG 桶，`can_short=False` |
| 永续单向 | `{(LONG,...): 2.0}` 或 `{(SHORT,...): 1.0}` | +2.0 / -1.0 | 二者互斥，不会同时非零 |
| 永续双向 | `{(LONG,...): 2.0, (SHORT,...): 1.0}` | +1.0 | **net_volume 不足以决策**，需分边看 |
| FX netting | `{(LONG,...): 1.5}` | +1.5 lot | 同永续单向 |
| FX hedging | `{(LONG,ticket="1001",opened_at=T1): 0.5, (LONG,ticket="1002",opened_at=T2): 1.0}` | +1.5 lot | 平仓需选 ticket |

**关键结论**：`net_volume` 只在 ONE_WAY / NETTING 下是充分信息。HEDGE 模式必须用 `side_volume(LONG)` 与 `side_volume(SHORT)` 分别决策。Resolver 算法必须先按 `position_mode` 分支（见 §8.4、§9.3）。

---

## 4. 输入契约：OrderRequest

### 4.1 定义

```python
"""kq_engine/resolution/base.py"""
from __future__ import annotations

from dataclasses import dataclass
from decimal import Decimal
from enum import Enum
from typing import Optional


class VenueSide(str, Enum):
    BUY = "BUY"
    SELL = "SELL"


class OrderKind(str, Enum):
    MARKET = "MARKET"
    LIMIT = "LIMIT"
    STOP_MARKET = "STOP_MARKET"     # 由 stop_engine 触发后转为 MARKET
    STOP_LIMIT = "STOP_LIMIT"


class Tif(str, Enum):
    GTC = "GTC"
    IOC = "IOC"
    FOK = "FOK"
    POST_ONLY = "POST_ONLY"


@dataclass(frozen=True)
class OrderRequest:
    """策略端意图。约定 A（volume）与约定 B（side+qty）二选一。"""

    symbol: str
    strategy_id: str
    order_id: str                   # 内部主键，Router 生成

    # ── 约定 A：市场中性 ──
    volume: Optional[Decimal] = None        # signed，单位 = spec.qty_unit

    # ── 约定 B：市场显式 ──
    side: Optional[VenueSide] = None
    quantity: Optional[Decimal] = None      # 正数
    offset: Optional["Offset"] = None       # OPEN / CLOSE / AUTO
    reduce_only: bool = False               # 市场中性语义，保留为一等字段（见 §15.2）

    # ── venue 特定参数（§15.2，白名单校验）──
    # positionSide / workingType（币安）、type_time / comment（MT5）等
    venue_params: dict[str, Any] = field(default_factory=dict)

    # ── 数量别名（三选一，优先级见 §6.2）──
    lots: Optional[Decimal] = None          # 手数（FX 原生；crypto 需 spec 支持）
    shares: Optional[Decimal] = None        # 最小单位数（= qty_unit 原生量）
    notional: Optional[Decimal] = None      # 按金额（quote ccy）

    # ── 订单参数 ──
    order_kind: OrderKind = OrderKind.MARKET
    price: Optional[Decimal] = None         # LIMIT/STOP_LIMIT 必填
    stop_price: Optional[Decimal] = None
    tif: Tif = Tif.GTC
    post_only: bool = False

    # ── Router 注入 ──
    ref_price: Optional[Decimal] = None     # 市价单预检用参考价
    spec_ref_ts: int = 0                    # 取哪一版 spec（回测=bar ts）


class Offset(str, Enum):
    AUTO = "AUTO"           # 默认：由 Resolver 推断
    OPEN = "OPEN"           # 强制开仓
    CLOSE = "CLOSE"         # 强制平仓（不足则拒单，不跨零）
```

### 4.2 约定 A / B 的关系

| | 约定 A | 约定 B |
|---|---|---|
| 写法 | `volume=-2.5` | `side=SELL, quantity=2.5, offset=CLOSE` |
| 可移植性 | 跨市场同代码 | 市场特定 |
| 跨零 | 自动拆腿 | `offset=CLOSE` 拒单；`AUTO` 才拆 |
| 适用 | 默认路径 | 平今平昨、锁仓、指定 ticket |

两者归约到同一 `VenueOrderSpec`。**后端工作量相同**——约定 B 只是省掉推断，精度对齐/合规检查/腿意图标注一样不少。

### 4.3 `volume` 单位警示（FX 陷阱）

```python
api.send_order("BTC/USDT", volume=Decimal("1"))    # 1 BTC
api.send_order("EUR/USD",  volume=Decimal("1"))    # 1 LOT = 100,000 EUR  ← 不是 1 EUR
```

SDK 层必须在 `send_order` 文档字符串与首次调用时打印一次警示，并提供 `lots=` 别名让意图显式：

```python
api.send_order("EUR/USD", lots=Decimal("1"))       # 推荐：单位自解释
api.send_order("EUR/USD", shares=Decimal("100000"))# 明确给 base ccy 量
```

`shares` 与 `lots` 的换算：`lots = shares / spec.contract_multiplier`。

---

## 5. 输出契约

### 5.1 VenueOrderSpec

```python
class LegIntent(str, Enum):
    OPEN_LONG = "OPEN_LONG"
    CLOSE_LONG = "CLOSE_LONG"
    OPEN_SHORT = "OPEN_SHORT"
    CLOSE_SHORT = "CLOSE_SHORT"
    FLAT_LONG = "FLAT_LONG"       # 清零多头（reduce-only）
    FLAT_SHORT = "FLAT_SHORT"


@dataclass(frozen=True)
class VenueOrderSpec:
    """Resolver 唯一输出物。Venue 消费它，不感知任何市场语义。"""
    parent_order_id: str
    leg_index: int                    # 0-based；跨零/FIFO 拆单时 >0
    intent: LegIntent

    venue: str
    native_symbol: str
    side: VenueSide
    quantity: Decimal                 # venue 原生单位（已量化）
    order_kind: OrderKind
    price: Optional[Decimal]          # 已按 price_tick 对齐
    stop_price: Optional[Decimal]
    tif: Tif

    # ── 市场特定参数（None = 不适用）──
    reduce_only: Optional[bool]       # 永续
    position_side: Optional[str]      # 永续 HEDGE："LONG"/"SHORT"
    close_ticket: Optional[str]       # FX hedging 平指定持仓

    client_order_id: str              # 按 venue 格式生成，网络请求前已定
    requested_volume: Decimal         # 该腿对应的原始请求量（审计用）


@dataclass(frozen=True)
class Rejection:
    code: "RejectionCode"
    message: str
    retryable: bool = False
    detail: Optional[dict] = None


@dataclass(frozen=True)
class Resolution:
    specs: tuple[VenueOrderSpec, ...] = ()
    rejection: Optional[Rejection] = None
    warnings: tuple[str, ...] = ()

    @property
    def ok(self) -> bool:
        return self.rejection is None and len(self.specs) > 0

    @classmethod
    def reject(cls, code: "RejectionCode", msg: str,
               retryable: bool = False, **detail) -> "Resolution":
        return cls(rejection=Rejection(code, msg, retryable, detail or None))

    @classmethod
    def single(cls, spec: VenueOrderSpec,
               warnings: tuple[str, ...] = ()) -> "Resolution":
        return cls(specs=(spec,), warnings=warnings)
```

### 5.2 Resolver 协议

```python
from typing import Protocol


class Resolver(Protocol):
    product: ProductType

    def resolve(
        self,
        req: OrderRequest,
        state: PositionState,
        spec: InstrumentSpec,
    ) -> Resolution: ...


RESOLVERS: dict[ProductType, Resolver] = {
    ProductType.CRYPTO_SPOT: SpotResolver(),
    ProductType.CRYPTO_PERP_LINEAR: LinearPerpResolver(),
    ProductType.FX_CFD: FxResolver(),
}
```

---

## 6. 共享工具

### 6.1 量化

```python
"""kq_engine/resolution/_quant.py"""
from decimal import Decimal, ROUND_DOWN, ROUND_UP, ROUND_HALF_UP, localcontext
from enum import Enum


class RoundingMode(str, Enum):
    DOWN = "down"        # 向下：买入量、卖出平仓量（不超额、不超持仓）
    UP = "up"            # 向上：卖出限价（不压低价格）
    NEAREST = "nearest"  # 就近：止损触发价
    STRICT = "strict"    # ⛔ 不合规即抛 QuantityError，绝不静默调整（回测用）


class QuantityError(ValueError):
    """STRICT 模式下数量/价格不符合 step 时抛出"""


def quantize(value: Decimal, step: Decimal, *, mode: RoundingMode) -> Decimal:
    """唯一的量化入口。`mode` 必填且无默认值 —— 强制调用方表态。

    借鉴 ccxt 的 decimal_to_precision(value, precision, mode) 单一入口设计，
    但全程 Decimal（ccxt 内部用 float，见 §15.5）。
    """
    if step <= 0:
        return value

    with localcontext() as ctx:
        ctx.prec = 60
        ratio = value / step

        if mode is RoundingMode.STRICT:
            if ratio != ratio.to_integral_value():
                raise QuantityError(
                    f"{value} 不是 {step} 的整数倍（比值 {ratio}）"
                )
            return value

        rounding = {
            RoundingMode.DOWN: ROUND_DOWN,
            RoundingMode.UP: ROUND_UP,
            RoundingMode.NEAREST: ROUND_HALF_UP,
        }[mode]
        return ratio.to_integral_value(rounding=rounding) * step
```

**方向选择规则**（调用点必须显式写出 mode，review 时可直接看出对错）：

| 场景 | mode | 理由 |
|---|---|---|
| 买入开仓数量 | `DOWN` | 不超资金 |
| 卖出平仓数量 | `DOWN` | 不超持仓（超了会被交易所拒） |
| 限价买单价 | `DOWN` | 不追高 |
| 限价卖单价 | `UP` | 不压低 |
| 止损触发价 | `NEAREST` | 语义是"到达即触发" |
| **回测环境的全部量化** | `STRICT` | 精度问题在回测阶段暴露，不等实盘被拒 |

```python
# 调用示例
qty = quantize(abs(raw_volume), spec.qty_step, mode=RoundingMode.DOWN)
price = quantize(
    req.price, spec.price_tick,
    mode=RoundingMode.DOWN if side is VenueSide.BUY else RoundingMode.UP,
)
```

`STRICT` 由 Router 统一注入（回测环境默认 STRICT，实盘默认 DOWN），策略与 Resolver 无需感知——详见 §15.5。


### 6.2 体积归约

```python
def resolve_signed_volume(
    req: OrderRequest, spec: InstrumentSpec
) -> tuple[Decimal, Optional[Rejection]]:
    """把多种写法归约为 signed volume（单位 = spec.qty_unit）。

    优先级：volume > lots > shares > notional
    再按 side 施加符号（约定 B）。
    """
    raw: Optional[Decimal] = None
    source = ""

    if req.volume is not None:
        raw, source = req.volume, "volume"
    elif req.lots is not None:
        if not spec.qty_is_lot:
            return Decimal(0), Rejection(
                RejectionCode.LOT_SIZE_VIOLATION,
                f"lots= 仅适用于 lot 计价品种（{spec.symbol} 单位为 {spec.qty_unit}）",
            )
        raw, source = req.lots, "lots"
    elif req.shares is not None:
        with localcontext() as ctx:
            ctx.prec = 60
            raw = req.shares / spec.contract_multiplier
        source = "shares"
    elif req.notional is not None:
        price = req.price or req.ref_price
        if price is None or price <= 0:
            return Decimal(0), Rejection(
                RejectionCode.PRICE_REQUIRED,
                "notional= 需要 price 或 ref_price 才能换算数量",
            )
        with localcontext() as ctx:
            ctx.prec = 60
            raw = req.notional / price
        source = "shares/notional"

    if raw is None:
        return Decimal(0), Rejection(
            RejectionCode.ZERO_QUANTITY, "未提供任何数量参数"
        )

    # 约定 B：施加 side 符号
    if req.side is not None:
        raw = abs(raw) * (1 if req.side is VenueSide.BUY else -1)

    return raw, None
```

### 6.3 公共校验

```python
def validate_and_quantize(
    raw_volume: Decimal, spec: InstrumentSpec, req: OrderRequest,
    rounding: RoundingMode = RoundingMode.DOWN,
) -> tuple[Decimal, Optional[Rejection], tuple[str, ...]]:
    """量化 + 合规预检。返回 (final_volume, rejection, warnings)

    `rounding` 由 Router 注入：回测环境传 STRICT（精度问题立即暴露），
    实盘传 DOWN（保守量化后如实回报 requested vs filled）。
    """
    warnings: list[str] = []

    if raw_volume == 0:
        return Decimal(0), Rejection(
            RejectionCode.ZERO_QUANTITY, "请求量为 0"
        ), ()

    # ── 数量量化 ──
    try:
        qty = quantize(abs(raw_volume), spec.qty_step, mode=rounding)
    except QuantityError as e:
        # STRICT 模式：回测阶段就暴露精度不合规，不等实盘被拒
        return Decimal(0), Rejection(
            RejectionCode.LOT_SIZE_VIOLATION,
            f"STRICT 量化失败：{e}",
            detail={"step": str(spec.qty_step), "raw": str(raw_volume)},
        ), ()
    sign = 1 if raw_volume > 0 else -1

    if qty == 0:
        return Decimal(0), Rejection(
            RejectionCode.BELOW_MIN_QTY,
            f"量化后为 0：|{raw_volume}| < stepSize {spec.qty_step}",
            detail={"step": str(spec.qty_step), "raw": str(raw_volume)},
        ), ()

    if qty != abs(raw_volume):
        warnings.append(
            f"数量已量化 {raw_volume} → {sign * qty}（step={spec.qty_step}）"
        )

    if qty < spec.qty_min:
        return Decimal(0), Rejection(
            RejectionCode.BELOW_MIN_QTY,
            f"{qty} < qty_min {spec.qty_min}",
            retryable=True,
            detail={"qty": str(qty), "min": str(spec.qty_min)},
        ), ()

    if qty > spec.qty_max:
        return Decimal(0), Rejection(
            RejectionCode.EXCEEDS_MAX_QTY,
            f"{qty} > qty_max {spec.qty_max}",
            detail={"qty": str(qty), "max": str(spec.qty_max)},
        ), ()

    # ── 名义额预检（需参考价）──
    ref = req.price or req.ref_price
    if ref is not None and spec.min_notional > 0:
        notional = qty * ref * spec.contract_multiplier
        if notional < spec.min_notional:
            return Decimal(0), Rejection(
                RejectionCode.BELOW_MIN_NOTIONAL,
                f"名义额 {notional:.4f} {spec.quote_ccy} < min_notional "
                f"{spec.min_notional}",
                retryable=True,
                detail={
                    "notional": str(notional),
                    "min": str(spec.min_notional),
                    "ref_price": str(ref),
                },
            ), ()
    elif spec.min_notional > 0:
        warnings.append("无参考价，跳过 min_notional 预检（交易所仍可能拒单）")

    # ── 价格对齐 ──
    if req.order_kind in (OrderKind.LIMIT, OrderKind.STOP_LIMIT):
        if req.price is None:
            return Decimal(0), Rejection(
                RejectionCode.PRICE_REQUIRED, "限价单必须提供 price"
            ), ()

    return sign * qty, None, tuple(warnings)


def build_spec(
    req: OrderRequest, spec: InstrumentSpec, *,
    leg_index: int, intent: LegIntent,
    side: VenueSide, quantity: Decimal,
    reduce_only: Optional[bool] = None,
    position_side: Optional[str] = None,
    close_ticket: Optional[str] = None,
    client_order_id: str = "",
) -> VenueOrderSpec:
    """构造 VenueOrderSpec，统一处理价格对齐"""
    price = req.price
    if price is not None:
        # 买单向下、卖单向上，避免价格劣化
        price = quantize(
            price, spec.price_tick,
            mode=RoundingMode.DOWN if side is VenueSide.BUY else RoundingMode.UP,
        )

    return VenueOrderSpec(
        parent_order_id=req.order_id,
        leg_index=leg_index,
        intent=intent,
        venue=spec.venue,
        native_symbol=spec.native_symbol,
        side=side,
        quantity=quantity,
        order_kind=req.order_kind,
        price=price,
        stop_price=(quantize(req.stop_price, spec.price_tick,
                             mode=RoundingMode.NEAREST)
                    if req.stop_price is not None else None),
        tif=Tif.POST_ONLY if req.post_only else req.tif,
        reduce_only=reduce_only,
        position_side=position_side,
        close_ticket=close_ticket,
        client_order_id=client_order_id,
        requested_volume=quantity,
    )
```

---

## 7. SpotResolver

### 7.1 算法

```python
"""kq_engine/resolution/spot.py"""


class SpotResolver:
    """加密现货。不可做空，单向净持仓，无 reduceOnly。"""

    product = ProductType.CRYPTO_SPOT

    def resolve(self, req, state, spec) -> Resolution:
        if spec.status != "TRADING":
            return Resolution.reject(
                RejectionCode.INSTRUMENT_HALTED,
                f"{spec.symbol} 状态为 {spec.status}",
            )

        # ── Step 1: 归约 signed volume ──
        raw, rej = resolve_signed_volume(req, spec)
        if rej is not None:
            return Resolution(rejection=rej)

        # ── Step 2: offset=CLOSE 语义校验 ──
        if req.offset is Offset.CLOSE:
            raw = self._apply_close_only(raw, state, spec)
            if isinstance(raw, Resolution):
                return raw

        # ── Step 3: 量化 + 合规 ──
        qty, rej, warns = validate_and_quantize(raw, spec, req)
        if rej is not None:
            return Resolution(rejection=rej)

        # ── Step 4: 方向能力 ──
        current = state.net_volume
        target = current + qty

        if target < 0:
            return Resolution.reject(
                RejectionCode.CANNOT_SHORT,
                f"现货不可做空：current={current}, volume={qty}, target={target}",
                detail={"current": str(current), "requested": str(qty),
                        "target": str(target)},
            )

        if req.reduce_only and not spec.reduce_only_supported:
            warns = warns + (f"{spec.venue} 不支持 reduceOnly，已忽略",)

        # ── Step 5: 卖出时校验可平量（T+0 下 == |current|）──
        if qty < 0:
            avail = state.side_volume(Side.LONG)
            if abs(qty) > avail:
                return Resolution.reject(
                    RejectionCode.INSUFFICIENT_AVAILABLE,
                    f"卖出 {abs(qty)} 超过持仓 {avail}",
                    retryable=True,
                    detail={"requested": str(abs(qty)), "available": str(avail)},
                )

        # ── Step 6: 单腿输出 ──
        side = VenueSide.BUY if qty > 0 else VenueSide.SELL
        intent = LegIntent.OPEN_LONG if qty > 0 else LegIntent.CLOSE_LONG

        leg = build_spec(
            req, spec, leg_index=0, intent=intent,
            side=side, quantity=abs(qty),
            reduce_only=None,           # 现货不适用
            position_side=None,
            close_ticket=None,
            client_order_id=_make_client_id(spec, req.order_id, 0),
        )
        return Resolution.single(leg, warns)

    @staticmethod
    def _apply_close_only(raw, state, spec):
        """offset=CLOSE：只允许减仓到 0，不允许跨零或反向开仓"""
        current = state.net_volume
        if raw > 0:
            return Resolution.reject(
                RejectionCode.CANNOT_SHORT,
                "现货 offset=CLOSE 只支持卖出方向",
            )
        if abs(raw) > current:
            return Resolution.reject(
                RejectionCode.INSUFFICIENT_AVAILABLE,
                f"offset=CLOSE 请求平 {abs(raw)}，但持仓仅 {current}（不允许跨零）",
                retryable=True,
            )
        return raw
```

### 7.2 决策表

| current | volume | target | 结果 |
|---|---|---|---|
| 0 | +1.0 | +1.0 | 1 腿 `OPEN_LONG` BUY 1.0 |
| +1.0 | +0.5 | +1.5 | 1 腿 `OPEN_LONG` BUY 0.5 |
| +1.0 | -0.5 | +0.5 | 1 腿 `CLOSE_LONG` SELL 0.5 |
| +1.0 | -1.0 | 0 | 1 腿 `CLOSE_LONG` SELL 1.0 |
| +1.0 | -2.0 | -1.0 | **拒单 `CANNOT_SHORT`** |
| 0 | -1.0 | -1.0 | **拒单 `CANNOT_SHORT`** |
| +1.0 | 0 | +1.0 | **拒单 `ZERO_QUANTITY`** |
| +1.0 | +0.000001 | — | **拒单 `BELOW_MIN_QTY`**（量化后为 0） |

**现货永不超过 1 腿**——这是它比永续/FX 简单的根本原因（无跨零、无 ticket）。

---

## 8. LinearPerpResolver

### 8.1 算法

```python
"""kq_engine/resolution/linear_perp.py"""


class LinearPerpResolver:
    """U本位永续。可做空，支持单向/双向持仓，reduceOnly 语义。"""

    product = ProductType.CRYPTO_PERP_LINEAR

    def resolve(self, req, state, spec) -> Resolution:
        if spec.status != "TRADING":
            return Resolution.reject(
                RejectionCode.INSTRUMENT_HALTED,
                f"{spec.symbol} 状态为 {spec.status}",
            )

        if not spec.reduce_only_supported:
            return Resolution.reject(
                RejectionCode.UNSUPPORTED_POSITION_MODE,
                f"{spec.venue} 未声明 reduceOnly 支持，永续 Resolver 拒绝处理",
            )

        raw, rej = resolve_signed_volume(req, spec)
        if rej is not None:
            return Resolution(rejection=rej)

        if spec.position_mode is PositionMode.HEDGE:
            return self._resolve_hedge(req, state, spec, raw)
        return self._resolve_one_way(req, state, spec, raw)

    # ── 单向持仓 ──

    def _resolve_one_way(self, req, state, spec, raw) -> Resolution:
        if req.offset is Offset.CLOSE:
            r = self._close_only_one_way(raw, state)
            if isinstance(r, Resolution):
                return r
            raw = r

        qty, rej, warns = validate_and_quantize(raw, spec, req)
        if rej is not None:
            return Resolution(rejection=rej)

        current = state.net_volume
        target = current + qty
        legs: list[VenueOrderSpec] = []

        # ── 情形 1：空仓开仓 ──
        if current == 0:
            legs.append(self._open_leg(req, spec, 0, qty, warns))

        # ── 情形 2：同向加仓 ──
        elif _same_sign(current, target) and abs(target) > abs(current):
            legs.append(self._open_leg(req, spec, 0, qty, warns))

        # ── 情形 3：同向减仓 ──
        elif _same_sign(current, target) and abs(target) < abs(current):
            close_qty = abs(current) - abs(target)
            legs.append(self._close_leg(
                req, spec, 0, close_qty,
                side_of(current), LegIntent.CLOSE_LONG if current > 0
                                  else LegIntent.CLOSE_SHORT,
            ))

        # ── 情形 4：清零 ──
        elif target == 0:
            legs.append(self._close_leg(
                req, spec, 0, abs(current), side_of(current),
                LegIntent.FLAT_LONG if current > 0 else LegIntent.FLAT_SHORT,
            ))

        # ── 情形 5：跨零（拆两腿）──
        else:
            legs.append(self._close_leg(
                req, spec, 0, abs(current), side_of(current),
                LegIntent.FLAT_LONG if current > 0 else LegIntent.FLAT_SHORT,
            ))
            legs.append(self._open_leg(req, spec, 1, target, warns))
            warns = warns + (
                f"跨零拆单：平 {side_of(current).value} {abs(current)} + "
                f"开 {side_of(target).value} {abs(target)}",
            )

        return Resolution(specs=tuple(legs), warnings=tuple(warns))

    def _close_only_one_way(self, raw, state):
        """offset=CLOSE：只减仓，不跨零"""
        current = state.net_volume
        if current == 0:
            return Resolution.reject(
                RejectionCode.INSUFFICIENT_AVAILABLE,
                "offset=CLOSE 但当前空仓", retryable=True,
            )
        if not _same_sign(raw, -current):
            return Resolution.reject(
                RejectionCode.LOT_SIZE_VIOLATION,
                f"offset=CLOSE 方向错误：持仓 {current}，请求 {raw}",
            )
        if abs(raw) > abs(current):
            return Resolution.reject(
                RejectionCode.INSUFFICIENT_AVAILABLE,
                f"offset=CLOSE 请求 {abs(raw)} 超过持仓 {abs(current)}（不允许跨零）",
                retryable=True,
            )
        return raw

    # ── 双向持仓 ──

    def _resolve_hedge(self, req, state, spec, raw) -> Resolution:
        """HEDGE 模式：必须明确作用于哪一边。

        net_volume 在此模式下不足以决策（LONG 2.0 + SHORT 1.0 → net +1.0，
        但实际是锁仓状态）。
        """
        long_qty = state.side_volume(Side.LONG)
        short_qty = state.side_volume(Side.SHORT)

        # 决定作用边：从 venue_params 读（§15.2，position_side 已非一等字段）
        ps_raw = req.venue_params.get("positionSide")
        if ps_raw is not None and ps_raw not in ("LONG", "SHORT", "BOTH"):
            return Resolution.reject(
                RejectionCode.INVALID_VENUE_PARAM,
                f"positionSide 只能是 LONG/SHORT/BOTH，实际 {ps_raw!r}",
            )
        ps: Optional[Side] = None if ps_raw in (None, "BOTH") else Side(ps_raw)

        if ps is None:
            # 未指定时的推断规则
            if raw > 0:
                ps = Side.SHORT if short_qty > 0 and req.offset is Offset.CLOSE else Side.LONG
            else:
                ps = Side.LONG if long_qty > 0 and req.offset is Offset.CLOSE else Side.SHORT

        held = long_qty if ps is Side.LONG else short_qty

        if req.offset is Offset.CLOSE:
            if abs(raw) > held:
                return Resolution.reject(
                    RejectionCode.INSUFFICIENT_AVAILABLE,
                    f"HEDGE offset=CLOSE：{ps.value} 边持仓 {held}，请求 {abs(raw)}",
                    retryable=True,
                )
            close_qty = abs(raw)
            intent = (LegIntent.CLOSE_LONG if ps is Side.LONG
                      else LegIntent.CLOSE_SHORT)
            leg = self._close_leg(
                req, spec, 0, close_qty, ps.opposite, intent,
                position_side=ps.value,
            )
            return Resolution.single(leg)

        # AUTO / OPEN：在该边上加仓或减仓
        target_on_side = held + abs(raw) if _opening(raw, ps) else held - abs(raw)

        if _opening(raw, ps):
            leg = self._open_leg(
                req, spec, 0, raw, (), position_side=ps.value,
            )
            return Resolution.single(leg)

        if target_on_side < 0:
            return Resolution.reject(
                RejectionCode.INSUFFICIENT_AVAILABLE,
                f"HEDGE 模式 {ps.value} 边减仓 {abs(raw)} 超过持仓 {held}；"
                f"跨边需显式拆两次调用",
                retryable=True,
            )

        if target_on_side == 0:
            intent = (LegIntent.FLAT_LONG if ps is Side.LONG
                      else LegIntent.FLAT_SHORT)
            leg = self._close_leg(
                req, spec, 0, held, ps.opposite, intent,
                position_side=ps.value,
            )
        else:
            intent = (LegIntent.CLOSE_LONG if ps is Side.LONG
                      else LegIntent.CLOSE_SHORT)
            leg = self._close_leg(
                req, spec, 0, abs(raw), ps.opposite, intent,
                position_side=ps.value,
            )
        return Resolution.single(leg)

    # ── 腿构造 ──

    def _open_leg(self, req, spec, idx, signed_qty, warns,
                  position_side=None) -> VenueOrderSpec:
        side = VenueSide.BUY if signed_qty > 0 else VenueSide.SELL
        intent = LegIntent.OPEN_LONG if signed_qty > 0 else LegIntent.OPEN_SHORT
        return build_spec(
            req, spec, leg_index=idx, intent=intent,
            side=side, quantity=abs(signed_qty),
            reduce_only=False,
            position_side=position_side,
            client_order_id=_make_client_id(spec, req.order_id, idx),
        )

    def _close_leg(self, req, spec, idx, qty, venue_side, intent,
                   position_side=None) -> VenueOrderSpec:
        return build_spec(
            req, spec, leg_index=idx, intent=intent,
            side=venue_side, quantity=qty,
            reduce_only=True,          # ← 永续平仓必带
            position_side=position_side,
            client_order_id=_make_client_id(spec, req.order_id, idx),
        )


# ── 辅助 ──

def _same_sign(a: Decimal, b: Decimal) -> bool:
    return (a > 0 and b > 0) or (a < 0 and b < 0)


def side_of(signed: Decimal) -> Side:
    return Side.LONG if signed > 0 else Side.SHORT


def _opening(signed_volume: Decimal, acting_side: Side) -> bool:
    """在 acting_side 上，该 volume 是加仓还是减仓"""
    if acting_side is Side.LONG:
        return signed_volume > 0
    return signed_volume < 0
```

### 8.2 单向模式决策表

| current | volume | target | 腿数 | 输出 |
|---|---|---|---|---|
| 0 | +2.0 | +2.0 | 1 | `OPEN_LONG` BUY 2.0, reduceOnly=F |
| +1.0 | +1.5 | +2.5 | 1 | `OPEN_LONG` BUY 1.5, reduceOnly=F |
| +2.0 | -0.5 | +1.5 | 1 | `CLOSE_LONG` SELL 0.5, **reduceOnly=T** |
| +2.0 | -2.0 | 0 | 1 | `FLAT_LONG` SELL 2.0, reduceOnly=T |
| **+1.0** | **-2.5** | **-1.5** | **2** | 腿0 `FLAT_LONG` SELL 1.0 RO=T<br>腿1 `OPEN_SHORT` SELL 1.5 RO=F |
| -1.0 | +3.0 | +2.0 | 2 | 腿0 `FLAT_SHORT` BUY 1.0 RO=T<br>腿1 `OPEN_LONG` BUY 2.0 RO=F |
| 0 | -1.0 | -1.0 | 1 | `OPEN_SHORT` SELL 1.0, reduceOnly=F |

**跨零两腿的 side 相同**（都是 SELL 或都是 BUY），区别只在 `reduceOnly`。这是最容易写错的地方——如果腿1 漏了 `reduceOnly=False`，交易所会把它当平仓单，结果持仓变成 0 而非 -1.5，**且不报错**。

### 8.3 原子性

跨零两腿不是原子的。腿0 成交、腿1 被拒时：

- `Order.filled_volume` = 腿0 的量（-1.0），不是请求量（-2.5）
- `Order.terminal_reason` = `PARTIAL_REJECTED`
- EventLog 记录两腿各自的终态
- **Portfolio 按实际成交更新 buckets**，持仓变为 0（不是 -1.5）

绝不能因为"腿0 成功"就报告订单成功。这类静默错误持仓是实盘最危险的故障模式。

### 8.4 HEDGE 模式的处理策略

**建议 v1 不支持 HEDGE，`position_mode=HEDGE` 时直接拒单：**

```python
return Resolution.reject(
    RejectionCode.UNSUPPORTED_POSITION_MODE,
    "HEDGE 双向持仓在 v1 未实现；请在交易所侧切换为单向持仓",
)
```

理由：
1. HEDGE 下 `net_volume` 失去意义，所有依赖它的逻辑（风控敞口、PnL）都要改
2. 锁仓策略需要 `position_side` escape hatch，破坏市场无关性
3. 币安/OKX 默认单向，多数策略用不到
4. 上面 `_resolve_hedge` 的实现已覆盖主要分支，但**未经真实交易所验证**，贸然上线风险高

若确实需要（跨期套利、锁仓对冲），Phase 2 单独设计，并同步扩展 `RiskEngine` 的敞口口径。

---

## 9. FxResolver

> **已确认配置**：IC Markets / MetaTrader 5 / **Netting 账户**。
> hedging 分支（ticket FIFO 多腿）v1 **不启用**，仅保留骨架与拒单保护（§9.4）。

### 9.1 与永续单向的差异

Netting 确认后，FX 与永续单向持仓高度同构，差异集中在四处：

| | 永续单向 | FX CFD（Netting） |
|---|---|---|
| 持仓表达 | `net_volume` | `net_volume`（**相同**） |
| 数量单位 | base ccy（BTC） | **lot**（1 lot = 100,000 base ccy） |
| 平仓参数 | `reduce_only=True` | **无**（经纪商自动净轧） |
| 可能腿数 | 1~2 | 1~2（**相同**） |
| 持仓成本 | 资金费率（8h） | swap（每日，周三三倍） |
| 幂等键 | `clientOrderId` | **无**（须本地去重，§11.3） |
| 委托附加参数 | — | `filling_mode` / `deviation` / `magic` |

**结论**：Resolver 算法与 `LinearPerpResolver._resolve_one_way` 结构一致，只是 `reduce_only` 恒为 `None`、数量单位是 lot。这意味着可以共享大部分逻辑，但**不要合并成一个类**——两者的 venue 参数集与错误模式差异太大，合并后分支会失控。

### 9.2 算法

```python
"""kq_engine/resolution/fx.py"""


class FxResolver:
    """外汇 CFD（IC Markets MT5，Netting 账户）。

    主路径 _resolve_netting；hedging 分支 v1 直接拒单（§9.4）。
    """

    product = ProductType.FX_CFD

    def resolve(self, req, state, spec) -> Resolution:
        if spec.status != "TRADING":
            return Resolution.reject(
                RejectionCode.INSTRUMENT_HALTED,
                f"{spec.symbol} 状态为 {spec.status}",
            )

        # hedging 账户走另一条路（v1 未启用）
        if spec.position_mode is PositionMode.HEDGE:
            return Resolution.reject(
                RejectionCode.UNSUPPORTED_POSITION_MODE,
                "MT5 Hedging 账户在 v1 未实现；IC Markets 提供 Netting 账户，"
                "建议在经纪商侧切换",
            )

        raw, rej = resolve_signed_volume(req, spec)
        if rej is not None:
            return Resolution(rejection=rej)

        return self._resolve_netting(req, state, spec, raw)

    # ── NETTING：主路径 ──

    def _resolve_netting(self, req, state, spec, raw) -> Resolution:
        # ── Step 1: offset=CLOSE 语义校验（不跨零）──
        if req.offset is Offset.CLOSE:
            current = state.net_volume
            if current == 0:
                return Resolution.reject(
                    RejectionCode.INSUFFICIENT_AVAILABLE,
                    "offset=CLOSE 但当前空仓", retryable=True,
                )
            if not _same_sign(raw, -current):
                return Resolution.reject(
                    RejectionCode.LOT_SIZE_VIOLATION,
                    f"offset=CLOSE 方向错误：持仓 {current} lot，请求 {raw} lot",
                )
            if abs(raw) > abs(current):
                return Resolution.reject(
                    RejectionCode.INSUFFICIENT_AVAILABLE,
                    f"offset=CLOSE 请求 {abs(raw)} lot 超过持仓 {abs(current)} lot"
                    f"（不允许跨零）",
                    retryable=True,
                )

        # ── Step 2: lot 量化 + 合规 ──
        qty, rej, warns = validate_and_quantize(raw, spec, req)
        if rej is not None:
            return Resolution(rejection=rej)

        # FX 特有：量化后仍可能低于 min lot（qty_min 通常 0.01）
        if abs(qty) < spec.qty_min:
            return Resolution.reject(
                RejectionCode.BELOW_MIN_QTY,
                f"{abs(qty)} lot < volume_min {spec.qty_min} lot"
                f"（= {spec.qty_min * spec.contract_multiplier} {spec.base_ccy}）",
                retryable=True,
            )

        # ── Step 3: 目标持仓与腿构造 ──
        current = state.net_volume
        target = current + qty
        legs: list[VenueOrderSpec] = []

        if current == 0:
            # 空仓开仓
            legs.append(self._leg(
                req, spec, 0,
                intent=LegIntent.OPEN_LONG if qty > 0 else LegIntent.OPEN_SHORT,
                side=VenueSide.BUY if qty > 0 else VenueSide.SELL,
                quantity=abs(qty),
            ))

        elif _same_sign(current, target):
            opening = abs(target) > abs(current)
            if opening:
                intent = (LegIntent.OPEN_LONG if qty > 0
                          else LegIntent.OPEN_SHORT)
            elif target == 0:
                intent = (LegIntent.FLAT_LONG if current > 0
                          else LegIntent.FLAT_SHORT)
            else:
                intent = (LegIntent.CLOSE_LONG if current > 0
                          else LegIntent.CLOSE_SHORT)
            legs.append(self._leg(
                req, spec, 0, intent=intent,
                side=VenueSide.BUY if qty > 0 else VenueSide.SELL,
                quantity=abs(qty),
            ))

        else:
            # 跨零：拆两腿（MT5 netting 下经纪商会自动净轧，
            # 但显式拆腿便于逐腿归因与失败处理）
            legs.append(self._leg(
                req, spec, 0,
                intent=(LegIntent.FLAT_LONG if current > 0
                        else LegIntent.FLAT_SHORT),
                side=VenueSide.SELL if current > 0 else VenueSide.BUY,
                quantity=abs(current),
            ))
            legs.append(self._leg(
                req, spec, 1,
                intent=(LegIntent.OPEN_LONG if target > 0
                        else LegIntent.OPEN_SHORT),
                side=VenueSide.BUY if target > 0 else VenueSide.SELL,
                quantity=abs(target),
            ))
            warns = warns + (
                f"FX netting 跨零拆单：平 {abs(current)} lot + "
                f"开 {abs(target)} lot",
            )

        # ── Step 4: swap 成本提示 ──
        if spec.swap is not None and not spec.swap.swap_free:
            warns = warns + (
                f"隔夜融资费：long {spec.swap.long_per_lot}/lot/日, "
                f"short {spec.swap.short_per_lot}/lot/日"
                f"（周{spec.swap.triple_day}三倍）",
            )

        return Resolution(specs=tuple(legs), warnings=tuple(warns))

    def _leg(self, req, spec, idx, *, intent, side, quantity) -> VenueOrderSpec:
        """FX 腿构造：reduce_only / position_side / close_ticket 一律为 None"""
        return build_spec(
            req, spec, leg_index=idx, intent=intent,
            side=side, quantity=quantity,
            reduce_only=None,
            position_side=None,
            close_ticket=None,
            client_order_id=_make_client_id(spec, req.order_id, idx),
        )
```

### 9.3 Netting 决策表

`volume` 单位为 **lot**。

| current | volume | target | 腿数 | 输出 |
|---|---|---|---|---|
| 0 | +1.00 | +1.00 | 1 | `OPEN_LONG` BUY 1.00 |
| +1.00 | +0.50 | +1.50 | 1 | `OPEN_LONG` BUY 0.50 |
| +1.50 | -0.50 | +1.00 | 1 | `CLOSE_LONG` SELL 0.50 |
| +1.00 | -1.00 | 0 | 1 | `FLAT_LONG` SELL 1.00 |
| **+1.00** | **-2.50** | **-1.50** | **2** | 腿0 `FLAT_LONG` SELL 1.00<br>腿1 `OPEN_SHORT` SELL 1.50 |
| -1.00 | +3.00 | +2.00 | 2 | 腿0 `FLAT_SHORT` BUY 1.00<br>腿1 `OPEN_LONG` BUY 2.00 |
| 0 | -1.00 | -1.00 | 1 | `OPEN_SHORT` SELL 1.00 |

**与永续的关键区别**：所有腿 `reduce_only=None`。MT5 netting 下经纪商自动净轧，无需也没有这个参数。

**跨零两腿的 side 相同**（都 SELL 或都 BUY），与永续一致——但永续靠 `reduce_only` 区分开平，MT5 靠**提交顺序**（先平后开）。这意味着 §9.5 的顺序保证是硬要求，不是优化。

### 9.4 Hedging 分支（v1 不启用，参考实现）

> **IC Markets 提供 Netting 账户，本项目已确认使用 Netting。**
> 以下代码保留作为骨架，`resolve()` 遇 `HEDGE` 直接拒单（§9.2）。
> 若未来切换 hedging 账户，需同步实现：ticket 追踪、FIFO 顺序、`position_id` 显式平仓、
> 以及 §9.5 的多腿幂等去重。

```python
    # ── HEDGING：按 ticket FIFO 平仓，可能多腿 ──

    def _resolve_hedging(self, req, state, spec, raw) -> Resolution:
        qty, rej, warns = validate_and_quantize(raw, spec, req)
        if rej is not None:
            return Resolution(rejection=rej)

        # 指定 ticket 的显式平仓（venue_params["position"]，MT5 TradeRequest 原生键名）
        if req.venue_params.get("position") is not None:
            return self._close_specific_ticket(req, state, spec, qty)

        opening_side = Side.LONG if qty > 0 else Side.SHORT
        closing_side = opening_side.opposite
        remaining = abs(qty)
        legs: list[VenueOrderSpec] = []
        idx = 0

        # ── 阶段 1：平掉反向持仓（FIFO）──
        for bucket, bucket_qty in state.buckets_of(closing_side):
            if remaining <= 0:
                break
            take = min(remaining, bucket_qty)
            take = quantize(take, spec.qty_step, mode=RoundingMode.DOWN)
            if take <= 0:
                continue

            legs.append(build_spec(
                req, spec, leg_index=idx,
                intent=(LegIntent.CLOSE_LONG if closing_side is Side.LONG
                        else LegIntent.CLOSE_SHORT),
                side=(VenueSide.SELL if closing_side is Side.LONG
                      else VenueSide.BUY),
                quantity=take,
                reduce_only=None,
                close_ticket=bucket.ticket,     # ← hedging 关键参数
                client_order_id=_make_client_id(spec, req.order_id, idx),
            ))
            remaining -= take
            idx += 1

        if idx > 1:
            warns = warns + (
                f"按开仓时间顺序（opened_at 升序）平掉 {idx} 个持仓",
            )

        # ── 阶段 2：余量开新仓 ──
        if remaining > 0:
            remaining = quantize(remaining, spec.qty_step, mode=RoundingMode.DOWN)
            if remaining >= spec.qty_min:
                legs.append(build_spec(
                    req, spec, leg_index=idx,
                    intent=(LegIntent.OPEN_LONG if qty > 0
                            else LegIntent.OPEN_SHORT),
                    side=VenueSide.BUY if qty > 0 else VenueSide.SELL,
                    quantity=remaining,
                    reduce_only=None,
                    close_ticket=None,          # 开新仓，无 ticket
                    client_order_id=_make_client_id(spec, req.order_id, idx),
                ))
                idx += 1
            elif remaining > 0:
                warns = warns + (
                    f"余量 {remaining} lot < min_lot {spec.qty_min}，已丢弃"
                    f"（不会开仓）",
                )

        if not legs:
            return Resolution.reject(
                RejectionCode.ZERO_QUANTITY,
                "量化后无可执行腿",
            )

        return Resolution(specs=tuple(legs), warnings=tuple(warns))

    def _close_specific_ticket(self, req, state, spec, qty) -> Resolution:
        """venue_params["position"] 显式指定：只平该 ticket，不足则拒单"""
        raw_ticket = req.venue_params["position"]
        if not isinstance(raw_ticket, int):
            return Resolution.reject(
                RejectionCode.INVALID_VENUE_PARAM,
                f"MT5 position ticket 必须是 int，实际 {type(raw_ticket).__name__}",
            )
        target_ticket = str(raw_ticket)      # bucket.ticket 存字符串
        matched = [(b, q) for b, q in state.buckets.items()
                   if b.ticket == target_ticket]
        if not matched:
            return Resolution.reject(
                RejectionCode.UNKNOWN_TICKET,
                f"持仓票号 {target_ticket} 不存在",
            )
        bucket, held = matched[0]
        want = abs(qty)
        if want > held:
            return Resolution.reject(
                RejectionCode.INSUFFICIENT_AVAILABLE,
                f"ticket {target_ticket} 持仓 {held} lot，请求平 {want} lot",
                retryable=True,
            )
        want = quantize(want, spec.qty_step, mode=RoundingMode.DOWN)

        leg = build_spec(
            req, spec, leg_index=0,
            intent=(LegIntent.CLOSE_LONG if bucket.side is Side.LONG
                    else LegIntent.CLOSE_SHORT),
            side=(VenueSide.SELL if bucket.side is Side.LONG
                  else VenueSide.BUY),
            quantity=want,
            reduce_only=None,
            close_ticket=target_ticket,
            client_order_id=_make_client_id(spec, req.order_id, 0),
        )
        return Resolution.single(leg)
```

#### Hedging FIFO 算例（参考，v1 不启用）

```
持仓（LONG 方向，按 opened_at 升序）:
  ticket=1001  0.50 lot  opened_at=T1
  ticket=1002  0.50 lot  opened_at=T2
  ticket=1003  1.00 lot  opened_at=T3

请求: send_order("EUR/USD", volume=-1.20)
  → qty = -1.20, closing_side = LONG, remaining = 1.20

  腿0: 平 ticket=1001  take=min(1.20, 0.50)=0.50   SELL 0.50  remaining=0.70
  腿1: 平 ticket=1002  take=min(0.70, 0.50)=0.50   SELL 0.50  remaining=0.20
  腿2: 平 ticket=1003  take=min(0.20, 1.00)=0.20   SELL 0.20  remaining=0.00

  → 3 腿，无开仓腿
  → ticket=1003 剩余 0.80 lot

请求: send_order("EUR/USD", volume=-2.50)
  腿0: 平 1001  0.50
  腿1: 平 1002  0.50
  腿2: 平 1003  1.00     remaining = 2.50 - 2.00 = 0.50
  腿3: 开 SHORT 0.50     ← 新开 ticket，close_ticket=None

  → 4 腿
```

**注意 `leg_index` 与 `client_order_id` 必须唯一。** REST 交易所会因重复 clientOrderId 拒单；但 **MT5 没有这个概念**，重复提交会真的下两单——见 §9.5。

### 9.5 MT5 提交语义（与 REST 交易所的关键差异）

Netting 下 FX 与永续算法同构，但**提交层完全不同**。这四点必须落实，否则实盘会出问题：

#### ① 无幂等键 → 必须本地去重

MT5 的 `order_send` 不接受 clientOrderId，`comment` 字段可写但**不参与去重**。

```python
# 提交前：本地去重表（DuckDB，与 EventLog 同事务）
if dedup.exists(client_order_id):
    return  # 已提交过，直接返回，绝不重发

event_log.append(VenueOrderSubmitted(client_order_id=..., ...))  # 先落盘
dedup.insert(client_order_id)                                     # 同事务

result = mt5.order_send(request)   # 再发网络
```

**顺序必须是「先落盘，后发网络」**。反过来（先发送再记录）在崩溃时会重复下单——这是 MT5 侧最危险的故障模式，REST 交易所靠 clientOrderId 兜底，MT5 只能靠本地。

`comment` 字段建议写入 `client_order_id`，配合 `mt5_magic` 过滤回报，便于人工对账。

#### ② 净轧靠提交顺序 → 跨零两腿必须串行

§9.3 的跨零拆单（腿0 平 1.00 + 腿1 开 1.50）在 MT5 netting 下**不能并发提交**。原因：netting 是「新单与现有净持仓自动轧差」，若两腿并发，第二腿可能先到达，轧差结果与预期相反。

```
正确：await submit(leg0); 确认回报; await submit(leg1)
错误：await asyncio.gather(submit(leg0), submit(leg1))
```

且腿0 失败时**腿1 不得提交**，`Order.terminal_reason = PARTIAL_REJECTED`。

对比永续：交易所靠 `reduce_only` 标志区分开平，两腿可并发。这是 FX 独有的顺序约束。

#### ③ filling_mode 必须按 Spec 传

`order_send` 的 `type_filling` 填错会被直接拒（retcode 10030 `TRADE_RETCODE_INVALID_FILLING`）。取值来自 `symbol_info().filling_mode` 位掩码：

| 位 | 模式 | 常量 |
|---|---|---|
| 1 | FOK | `ORDER_FILLING_FOK` |
| 2 | IOC | `ORDER_FILLING_IOC` |
| 4 | RETURN | `ORDER_FILLING_RETURN` |

IC Markets 通常支持 IOC，但**必须实测**（不同服务器配置可能不同），写入 `Spec.mt5_filling`。

#### ④ 同步阻塞 → 必须线程池隔离

所有 `mt5.*` 调用阻塞事件循环。7×24 实盘中，一次慢查询（如 `history_deals_get` 拉全量历史）会卡住**所有品种**的行情处理，导致其他市场的止损延迟触发。

```python
# ✗ 错误：阻塞事件循环
result = mt5.order_send(request)

# ✓ 正确：隔离到专用单线程执行器
_MT5_EXECUTOR = ThreadPoolExecutor(max_workers=1, thread_name_prefix="mt5")

result = await asyncio.get_running_loop().run_in_executor(
    _MT5_EXECUTOR, mt5.order_send, request
)
```

**`max_workers=1` 是刻意的**：MT5 Python 包不是线程安全的，多线程并发调用会导致未定义行为。所有 MT5 调用必须串行化到同一线程——这也顺带保证了 ② 的提交顺序。

#### ⑤ 回报获取：轮询 vs 本地 EA

MT5 Python 包**没有推送通道**，成交回报只能轮询 `history_deals_get()` / `positions_get()`。

| 方案 | 延迟 | 复杂度 |
|---|---|---|
| 轮询（`positions_get` 差量比对） | 100~500ms | 低 |
| 本地 MQL5 EA + socket 推送 | <50ms | 高（需额外进程） |

v1 建议轮询，间隔 200ms，与 crypto 的 WS 推送统一到同一 EventLog 事件类型。轮询逻辑放 `venue_lane`，不进 Resolver。

### 9.6 FX CFD 特有风险

| 风险 | 处理 |
|---|---|
| lot 单位误解（1 lot ≠ 1 EUR） | SDK 警示 + `lots=` 别名 + `qty_unit` 字段 |
| pip 因货币对而异（JPY 对例外） | Spec 显式 `pip_size`，禁止从 `price_tick` 推导 |
| **无幂等键，重复提交会真实下单** | 本地去重表，先落盘后发送（§9.5①） |
| **netting 净轧依赖提交顺序** | 跨零两腿串行，腿0 失败则腿1 不发（§9.5②） |
| **同步 API 阻塞事件循环** | 单线程执行器隔离（§9.5④） |
| filling_mode 不符被拒 | Spec 实测 `symbol_info().filling_mode`，写断言测试 |
| 跨零两腿部分成交 | `Order.filled_volume` 累加，`PARTIAL_REJECTED` 终态 |
| swap 单位由 `swap_mode` 决定 | loader 必须读 `swap_mode` 换算，否则差数量级 |
| swap 周三三倍 | `SwapModel.triple_day`，Portfolio 计提时应用 |
| Standard 账户成本全在点差 | 回测撮合必须用 bid/ask，不能用 mid |
| 杠杆因监管实体而异（30 vs 500） | 从 `account_info().leverage` 实测，不硬编码 |
| MT5 仅 Windows + 需终端常驻 | 部署前置检查，启动时校验终端连接与登录态 |


---

## 10. 端到端数值算例

### 10.1 现货：精度量化 + minNotional

```
Spec: BTCUSDT_SPOT (stepSize=0.00001, minNotional=5 USDT)
ref_price = 65,000 USDT

请求: api.send_order("BTC/USDT", volume=Decimal("0.001234"))

Step 1  resolve_signed_volume → raw = +0.001234
Step 2  quantize(0.001234, 0.00001, mode=DOWN)
        = (0.001234 / 0.00001).to_integral(DOWN) * 0.00001
        = 123 * 0.00001 = 0.00123
        warning: "数量已量化 0.001234 → 0.00123"
Step 3  qty_min 0.00001 <= 0.00123 <= 9999  ✓
Step 4  notional = 0.00123 × 65000 × 1 = 79.95 USDT >= 5  ✓
Step 5  current=0, target=+0.00123, 非负 ✓
Step 6  side=BUY, intent=OPEN_LONG

输出 1 腿:
  VenueOrderSpec(
    leg_index=0, intent=OPEN_LONG,
    venue="binance_spot", native_symbol="BTCUSDT",
    side=BUY, quantity=0.00123,
    order_kind=MARKET, price=None,
    reduce_only=None, position_side=None, close_ticket=None,
  )

Order.requested_volume = 0.001234   ← 策略请求的
Order.filled_volume    = 0.00123    ← 成交后回填（可能再少，若部分成交）
```

### 10.2 现货：minNotional 拒单

```
ref_price = 65,000

请求: api.send_order("BTC/USDT", volume=Decimal("0.00005"))

Step 2  quantize → 0.00005（正好整倍）
Step 4  notional = 0.00005 × 65000 = 3.25 USDT < 5

拒单: BELOW_MIN_NOTIONAL
  message: "名义额 3.2500 USDT < min_notional 5"
  retryable: True
  detail: {notional: "3.25", min: "5", ref_price: "65000"}

→ 不发网络请求，EventLog 记录 OrderRejected
```

### 10.3 永续：跨零拆单

```
Spec: BTCUSDT_PERP (stepSize=0.001, ONE_WAY, reduceOnly 支持)
持仓: current = +1.000 (LONG 1 BTC)

请求: api.send_order("BTC/USDT:PERP", volume=Decimal("-2.5"))

Step 1  raw = -2.5
Step 2  quantize(2.5, 0.001, mode=DOWN) = 2.500 → qty = -2.500
Step 3  current=+1.000, target=-1.500 → 符号相反 → 跨零

腿0: FLAT_LONG
     side=SELL, quantity=1.000, reduce_only=True
     client_order_id="kqbf-<order_id[:12]>-L0"
腿1: OPEN_SHORT
     side=SELL, quantity=1.500, reduce_only=False    ← 必须 False
     client_order_id="kqbf-<order_id[:12]>-L1"

warning: "跨零拆单：平 LONG 1.000 + 开 SHORT 1.500"

两腿 side 都是 SELL，仅 reduce_only 区分。
若腿1 的 reduce_only 误设为 True → 交易所当平仓处理 → 持仓变 0 而非 -1.5，静默错误。
```

### 10.4 永续：部分成交导致 PARTIAL_REJECTED

```
腿0 (平多 1.000) → 全部成交
腿1 (开空 1.500) → 交易所拒单（保证金不足）

结果:
  Order.status = DONE
  Order.terminal_reason = PARTIAL_REJECTED
  Order.requested_volume = -2.500
  Order.filled_volume = -1.000
  Portfolio: buckets 变为 {} （净持仓 0）

  绝不是 -1.500。策略必须读 filled_volume 更新认知。
```

### 10.5 外汇 netting：lot 与 pip

```
Spec: EURUSD_IC_RAW (qty_step=0.01, contract_multiplier=100000,
                     price_tick=0.00001, pip_size=0.0001, NETTING,
                     leverage_max=30)
ref_price = 1.08500

请求: api.send_order("EUR/USD", lots=Decimal("1.5"))

Step 1  resolve_signed_volume: lots 分支 → raw = +1.5 (lot)
Step 2  quantize(1.5, 0.01, mode=DOWN) = 1.50 ✓
Step 3  qty_min 0.01 <= 1.50 <= 500 ✓
Step 4  min_notional=0，跳过
Step 5  current=0 → intent=OPEN_LONG, side=BUY

输出 1 腿:
  quantity=1.50 (lot)   ← venue 原生单位是 lot，不是 150000 EUR
  native_symbol="EURUSDm"   ← ⚠️ 后缀由经纪商决定，接入时用 mt5.symbols_get() 实测

实际敞口 = 1.50 × 100000 = 150,000 EUR
名义额(USD) = 150,000 × 1.08500 = 162,750 USD
保证金(30:1, ASIC 实体) = 162,750 / 30 = 5,425 USD
  ↑ 若账户是离岸实体(500:1)则为 325.50 USD —— 差 16 倍，
    故 leverage_max 必须从 account_info().leverage 实测（§14.4）

若策略想止损 20 pips:
  stop = 1.08500 - 20 × 0.0001 = 1.08300
  quantize(1.08300, 0.00001, mode=NEAREST) = 1.08300 ✓

佣金(Raw Spread, PER_LOT $3.50/边) = 1.50 × 3.50 × 2 = 10.50 USD 往返
```

### 10.6 外汇 USDJPY：pip 例外

```
Spec: USDJPY_IC (price_tick=0.001, pip_size=0.01, price_decimals=3)
ref_price = 149.850

止损 30 pips:
  ✗ 错误: 149.850 - 30 × 0.0001 = 149.847   （用了 EURUSD 的 pip）
  ✓ 正确: 149.850 - 30 × 0.01   = 149.550

  quantize(149.550, 0.001, mode=NEAREST) = 149.550 ✓
```

这就是 `pip_size` 必须显式存储、不能从 `price_tick × 10` 推导的原因——虽然本例中 `0.001 × 10 = 0.01` 恰好成立，但 4 位/2 位老式报价下 `pip == price_tick`，推导会错。

### 10.7 外汇 hedging：FIFO 多腿

见 §9.3。要点：`close_ticket` 逐腿不同，`client_order_id` 逐腿唯一。

---

## 11. 错误码

```python
class RejectionCode(str, Enum):
    # ── 请求本身非法（策略 bug，不可重试）──
    ZERO_QUANTITY = "ZERO_QUANTITY"
    PRICE_REQUIRED = "PRICE_REQUIRED"
    LOT_SIZE_VIOLATION = "LOT_SIZE_VIOLATION"
    CANNOT_SHORT = "CANNOT_SHORT"
    INSTRUMENT_HALTED = "INSTRUMENT_HALTED"
    UNSUPPORTED_POSITION_MODE = "UNSUPPORTED_POSITION_MODE"
    MISSING_POSITION_SIDE = "MISSING_POSITION_SIDE"
    UNKNOWN_TICKET = "UNKNOWN_TICKET"

    # ── venue 透传参数非法（§15.2 白名单）──
    UNKNOWN_VENUE_PARAM = "UNKNOWN_VENUE_PARAM"      # 键不在白名单
    INVALID_VENUE_PARAM = "INVALID_VENUE_PARAM"      # 类型不符 / venue 不接受

    # ── 规格不符（可调整后重试）──
    BELOW_MIN_QTY = "BELOW_MIN_QTY"
    EXCEEDS_MAX_QTY = "EXCEEDS_MAX_QTY"
    BELOW_MIN_NOTIONAL = "BELOW_MIN_NOTIONAL"
    PRICE_TICK_VIOLATION = "PRICE_TICK_VIOLATION"

    # ── 状态相关（可能可重试）──
    INSUFFICIENT_AVAILABLE = "INSUFFICIENT_AVAILABLE"
    INSUFFICIENT_MARGIN = "INSUFFICIENT_MARGIN"      # 风控层产生
    PRICE_LIMIT_VIOLATION = "PRICE_LIMIT_VIOLATION"  # 涨跌停 / MT5 requote / Phase 2 A股
    ORDER_NOT_FOUND = "ORDER_NOT_FOUND"              # 撤单/查单时目标已不存在

    # ── 传输层（需对账后决定）──
    LOCAL_FAILED = "LOCAL_FAILED"        # 超时/网络，交易所侧状态未知
    VENUE_REJECTED = "VENUE_REJECTED"    # 交易所明确拒绝，含原生错误码
```

**`ORDER_NOT_FOUND` 的特殊性**：撤单时收到它通常是**幂等安全**的（单子已经成交或被撤），不应当作错误。MT5 的 `RETCODE_POSITION_CLOSED` 也映射到它（§15.3）。Router 对此码应静默成功而非告警。


**分类原则**：

| 类别 | 是否重试 | 是否告警 | 备注 |
|---|---|---|---|
| 请求非法 | ✗ | 记日志 | 策略代码 bug，重试无意义 |
| 规格不符 | 可调整后重试 | warning | 常见于 minNotional |
| 状态相关 | 可重试 | warning | 持仓/资金变化后可能通过 |
| `LOCAL_FAILED` | **必须先对账** | **critical** | 订单可能已在交易所创建 |

`LOCAL_FAILED` 是最危险的一类：超时不等于失败。处理流程：

```
submit 超时 → Order.status 保持 WORKING（不是 DONE）
           → client_order_id 已预先确定并落库
           → Reconciler 用 client_order_id 反查交易所
           → 查到 → 补回填真实状态
           → 查不到 → 确认未创建，标 LOCAL_FAILED
```

现有 [order_manager.py](<F:\KlineQuantWork\klinequant\core\trade_engine\order_manager.py>) 第 108-114 行把 submit 异常一律标 `FAILED`，会导致重复下单或漏对账，必须改。

---

## 12. 测试矩阵

### 12.1 Resolver 单元测试（纯函数，易穷举）

三个 Resolver 共用（差异见备注列）：

| # | 用例 | 断言 |
|---|---|---|
| 1 | 空仓开多/开空 | 1 腿，intent 正确（现货无开空） |
| 2 | 同向加仓 | 1 腿，OPEN_* |
| 3 | 同向减仓 | 1 腿，CLOSE_*；**永续 reduce_only=True，FX 为 None** |
| 4 | 清零 | 1 腿，FLAT_* |
| 5 | **跨零** | 2 腿，**两腿 side 相同**；永续腿1 reduce_only=False，FX 两腿均 None |
| 6 | 量化后为 0 | 拒单 BELOW_MIN_QTY |
| 7 | minNotional 不足 | 拒单，不发网络（FX min_notional=0，跳过此项） |
| 8 | 现货做空 | 拒单 CANNOT_SHORT（仅 SpotResolver） |
| 9 | offset=CLOSE 超额 | 拒单，不拆腿 |
| 10 | offset=CLOSE 方向错 | 拒单 |
| 11 | 价格未对齐 | 输出已对齐（买单 DOWN / 卖单 UP） |
| 12 | position_mode=HEDGE | **永续与 FX 均拒单** UNSUPPORTED_POSITION_MODE |

FX 额外：

| # | 用例 | 断言 |
|---|---|---|
| 13 | `lots=` vs `shares=` vs `volume=` 等价 | 三者产出相同 quantity（`shares=100000` == `lots=1`） |
| 14 | `lots=` 用在 crypto | 拒单 LOT_SIZE_VIOLATION |
| 15 | USDJPY pip 止损价 | 用 `pip_size=0.01` 而非 0.0001 |
| 16 | volume_min 边界 | `lots=0.005` 量化后为 0 → 拒单 BELOW_MIN_QTY |
| 17 | swap 提示 | 非 swap_free 时 warnings 含隔夜费 |
| 18 | native_symbol 映射 | 输出 `"EURUSDm"` 而非中性 `"EUR/USD"` |

FX hedging 分支（**v1 不启用，代码保留则测试标记 skip**）：

| # | 用例 | 断言 |
|---|---|---|
| H1 | FIFO 平 1 个 ticket | 1 腿，close_ticket 正确 |
| H2 | FIFO 跨多 ticket | N 腿，顺序按 opened_at 升序 |
| H3 | 平完再开反向 | N+1 腿，末腿 close_ticket=None |
| H4 | 指定 position_id 平 | 1 腿，只动该 ticket |
| H5 | position_id 不存在 | 拒单 UNKNOWN_TICKET |

### 12.2 一致性测试（进 CI，防回测/实盘分叉）

| # | 测试 | 防住的破裂 |
|---|---|---|
| C1 | 同一 `(req, state, spec)` 在 BacktestVenue 与 LiveVenue(mock) 走完整链路，EventLog 事件序列同构 | 两个 Venue 不可互换 |
| C2 | Resolver 输出对 `dict` 遍历顺序不敏感（buckets 用不同插入顺序构造，结果相同） | 非确定性 |
| C3 | 回测用历史 spec 与用当前 spec，拒单集合差异被显式记录 | spec 时间快照失效 |
| C4a | **永续**跨零两腿中腿1 的 `reduce_only` 恒为 `False`（hypothesis 属性测试） | 静默错误持仓 |
| C4b | **FX** 所有腿的 `reduce_only` / `position_side` / `close_ticket` 恒为 `None` | netting 下误传参数被经纪商拒单 |
| C5 | `Order.filled_volume` 恒等于各腿实际成交量之和（含部分成交、拒单混合） | 持仓与订单不一致 |
| C6 | FX `lots=1` 与 `shares=100000` 与 `volume=1` 产出字节相同的 VenueOrderSpec | 单位换算分歧 |
| C7 | **MT5 跨零两腿串行**：腿0 未确认回报前腿1 不提交；腿0 失败则腿1 不发 | netting 净轧顺序错乱 |
| C8 | **MT5 幂等**：同一 client_order_id 重复提交只发一次网络请求 | 重复下单（MT5 无 venue 侧去重） |
| C9 | **MT5 线程隔离**：所有 `mt5.*` 调用在同一线程执行 | Python 包非线程安全导致未定义行为 |

**C4a 是本次设计最高价值的测试**——跨零腿1 漏设 `reduce_only=False` 会造成静默错误持仓，且不报错，人工测试极难发现。建议用 hypothesis 随机生成 current ∈ [-10, 10]、volume ∈ [-10, 10] 的 Decimal 组合穷举。

**C7、C8 是 MT5 独有的**，REST 交易所不需要（有 clientOrderId 兜底 + 可并发）。这两条不做，FX 实盘一定会出重复单或净轧错乱。

### 12.3 回测/实盘同构验证

```python
def test_same_signal_same_events():
    """同一 Signal 序列，回测与实盘(mock) 产生同构事件流"""
    signals = [...]  # 固定脚本

    bt_events = run_with(BacktestVenue(matcher), signals)
    live_events = run_with(MockLiveVenue(replay=fixed_replies), signals)

    assert [e.type for e in bt_events] == [e.type for e in live_events]
    assert [e.payload["filled_volume"] for e in bt_events if e.type == "OrderDone"] \
         == [e.payload["filled_volume"] for e in live_events if e.type == "OrderDone"]
```

这要求 **Portfolio 在 Venue 之外**（前一轮已定），否则回测的 BacktestVenue 不建模持仓，事件序列必然不同。

---

## 13. 文件清单

```
kq_engine/
├── instruments/
│   ├── spec.py              InstrumentSpec / ProductType / PositionMode / FeeModel / SwapModel
│   ├── capability.py        ★ Capability 枚举 + CapabilitySet（§15.1）
│   ├── registry.py          Ref → VenueKey + Spec；按 ts 取历史版本
│   └── loaders/
│       ├── binance.py       exchangeInfo → Spec（含 filters 解析、precisionMode 消化）
│       ├── okx.py           instruments → Spec
│       └── mt5.py           symbol_info → Spec（lot/pip/swap_mode/filling_mode）
│
├── portfolio/
│   ├── state.py             PositionState / PositionBucket / Side / Age
│   ├── books.py             按 strategy_id 分账
│   └── projection.py        volume / available / avg_price / pnl
│
├── resolution/              ← 本文档核心
│   ├── base.py              Resolver 协议 / OrderRequest / Resolution
│   │                        VenueOrderSpec / LegIntent / RejectionCode
│   ├── _quant.py            ★ quantize 单一入口 + RoundingMode + 体积归约（§6.1）
│   ├── spot.py              SpotResolver
│   ├── linear_perp.py       LinearPerpResolver
│   ├── fx.py                FxResolver（netting 主路径）
│   └── ids.py               _make_client_id（按 venue 格式）
│
├── venues/
│   ├── base.py              ExecutionVenue 协议（消费 VenueOrderSpec，产出 Fill 事件）
│   ├── descriptor.py        ★ VenueDescriptor + SandboxConfig（§15.4）
│   ├── error_map.py         ★ VenueErrorMap + needs_reconcile（§15.3）
│   ├── param_whitelist.py   ★ venue_params 白名单校验（§15.2）
│   ├── backtest_matcher.py  回测撮合（bid/ask 模型）
│   ├── fake.py              假 venue（Week 2-3 用，可进 CI）
│   └── mt5/                 见 §13.3（connection / thread_bridge / dedup / poller …）
│
└── execution/
    ├── router.py            校验 → Spec 查询 → Resolver → 风控 → 分发
    │                        ★ 注入 RoundingMode；venue_params 白名单校验入口
    ├── venue_lane.py        per venue：限频桶、串行化、连接
    │                        ★ 按 Capability 决定推送 or 轮询（§15.1）
    ├── reconciler.py        LOCAL_FAILED / needs_reconcile 对账
    └── stop_engine.py       本地止损（跨市场一致）
```

★ = 本轮借鉴 ccxt 后新增（§15）

### 13.1 现有代码处置

| 文件 | 处置 |
|---|---|
| `protocol/types.py` `OrderStatus` / `_VALID_TRANSITIONS` | **保留**，改为 lifecycle + terminal_reason 双维度 |
| `protocol/types.py` `Order` | **拆三层**：Order / VenueOrder / Fill |
| `protocol/types.py` `Position` | **重写**：加 buckets，`exchange` 拆为 `venue` + `product` |
| `core/trade_engine/position_manager.py` | **删除**，由 `portfolio/state.py` 替代（单键索引 + `quantity==0` 就 pop 会误删跨市场持仓） |
| `core/trade_engine/order_manager.py` | **拆分**：状态机 → `resolution/base.py`；字典 → ObjectStore |
| `core/trade_engine/engine.py` | **删除** `process_signal` 中硬编码 `exchange="binance"`，逻辑并入 Router |
| `core/trade_engine/executors/*.py` | **改造**：返回值从 `Order` 改为 `Fill` 事件；不得构造新 Order |
| `gateway/routers/trade.py` 直连币安 REST 下单 | **删除**，必须走 Router（当前绕过风控与落库） |
| `storage/repositories/order_repository.py` | **改造**：适配三层模型，`float()` → Decimal |
| `storage/schema.py` orders 表 | **拆表**：orders / venue_orders / fills；`DOUBLE` → `DECIMAL(38,18)` |
| `storage/schema.py` | **新增**：instrument_specs（§2.3）、position_buckets |
| `core/backtest_engine/engine.py` 第 136-218 行 | **删除**全部内联持仓/PnL，只留 BacktestVenue |
| `gateway/market_sources/binance_source.py` `_parse_exchange_info` | **扩展**：解析 filters（LOT_SIZE/PRICE_FILTER/MIN_NOTIONAL）写入 Spec |
| `core/market_engine/normalizer.py` | **保留**，符号规范与 Spec.native_symbol 对齐 |

### 13.2 client_order_id 生成

```python
"""kq_engine/resolution/ids.py"""

_VENUE_PREFIX = {
    "binance_spot": "kqbs",
    "binance_futures": "kqbf",
    "okx": "kqox",
    "icmarkets_mt5": "kqmt",
}

def _make_client_id(spec: InstrumentSpec, order_id: str, leg_index: int) -> str:
    """按 venue 生成，腿间唯一。

    约束（需按官方文档核实并在此固化）：
      - 币安：^[\\.A-Za-z0-9_-]{1,36}$
      - OKX：字母数字，1-32 字符
      - MT5：**无 clientOrderId 概念**。此 ID 仅用于框架内部关联与
        写入 order_send 的 comment 字段供人工对账，venue 不会用它去重。
        幂等由本地去重表保证（§9.5①）。
    """
    prefix = _VENUE_PREFIX.get(spec.venue, "kq")
    # order_id 取 hex 前 12 位，腿号后缀，总长控制在 32 内
    core = order_id.replace("-", "")[:12]
    return f"{prefix}-{core}-L{leg_index}"
```

⚠️ **MT5 的幂等性与 REST 交易所根本不同**：重复提交会真的下两单，交易所侧不会去重。FX 的幂等**必须**在框架侧用本地去重表实现（先落盘后发送），这是实盘安全前提，详见 §9.5①。

### 13.3 MT5 venue adapter 落位

MT5 的接入约束（同步阻塞、单线程、无推送、无幂等键）需要专门一层，不能与 REST 交易所共用基类实现：

```
kq_engine/
└── venues/
    ├── base.py                ExecutionVenue 协议（消费 VenueOrderSpec，产出 Fill 事件）
    ├── backtest_matcher.py    回测撮合（bid/ask 模型，见 §14.3#13）
    ├── binance_spot.py        REST + WS user stream
    ├── binance_futures.py
    └── mt5/
        ├── __init__.py
        ├── connection.py      initialize/login 生命周期、终端存活检查、断线重连
        ├── thread_bridge.py   单线程 ThreadPoolExecutor（max_workers=1）
        │                      所有 mt5.* 调用经此隔离，保证串行与安全
        ├── spec_loader.py     symbol_info → InstrumentSpec（含 swap_mode 换算）
        ├── order_sender.py    VenueOrderSpec → TradeRequest；filling_mode/deviation
        ├── dedup.py           本地幂等去重表（与 EventLog 同事务）
        └── poller.py          positions_get/history_deals_get 差量轮询 → Fill 事件
```

**四个必须实现的前置检查**（启动时，失败即拒绝启动而非运行时报错）：

| 检查 | 方式 | 失败处理 |
|---|---|---|
| Windows 平台 | `sys.platform == "win32"` | 明确报错：MT5 包仅发 Windows wheel |
| 终端已安装且可启动 | `mt5.initialize()` 返回值 | 报错并提示终端路径配置 |
| 账户已登录 | `mt5.account_info()` 非 None | 报错，提示需在终端登录 |
| 账户模式为 Netting | `account_info().trade_mode` / `mt5.ACCOUNT_TRADE_MODE_*` | **报错**：v1 不支持 Hedging（§9.2） |

第四项尤其重要——若账户实际是 hedging，`FxResolver` 会逐单拒单，但那时已经启动了。启动时校验能提前暴露。

**回报轮询节奏**：`positions_get()` + `history_deals_get()` 差量比对，间隔 200ms，经 `thread_bridge` 串行执行。轮询结果转成与 crypto WS 相同的事件类型写入 EventLog，使上层（Portfolio / 策略代理对象）无需区分 venue。

---

## 14. 待确认决策

### 14.1 已确认（外汇侧）

| # | 决策 | 结论 | 影响 |
|---|---|---|---|
| 1 | FX 经纪商与平台 | **IC Markets / MetaTrader 5** | 新增 `venues/mt5/` 适配器族（§13.3） |
| 2 | FX 账户模式 | **Netting（净额）** | hedging 分支不启用，FxResolver 与永续单向同构（§9.2） |
| 3 | FX 产品类型 | **CFD**（非现货） | `FX_CFD`；无 T+2 交割；swap 为融资费（§1、§2.1） |
| 4 | NFA FIFO | **不适用** | IC 不接受美国客户，`fifo_required` 字段已移除 |

### 14.2 仍需你确认

| # | 决策 | 选项 | 建议 |
|---|---|---|---|
| 5 | **账户类型** | Raw Spread / Standard | 决定 `FeeModel.kind`。Raw = `PER_LOT` $3.50/边；Standard = `SPREAD_ONLY` + 点差 0.8 pip。**回测撮合模型不同**（Standard 必须用 bid/ask） |
| 6 | **监管实体** | ASIC(澳) / CySEC(塞) / FSA(塞舌尔) / SCB(巴哈马) | 决定 `leverage_max`：ASIC 零售 30x，离岸可达 500x。**直接影响保证金与风控限额** |
| 7 | **是否 swap-free 账户** | 是 / 否 | Islamic 账户不收 swap 但收 holding fee，`SwapModel` 走不同字段 |
| 8 | **永续 HEDGE 是否 v1 支持** | 支持 / 拒单 | 建议拒单（§8.4）。币安/OKX 默认单向 |
| 9 | **`volume` 在 FX 下的单位** | lot（本文采用）/ base ccy | lot 是行业惯例但与 crypto 语义差 10 万倍。已在 §4.3 加警示 + `lots=` 别名 |

第 5、6 项可在 MT5 终端里直接查到（账户类型看开户邮件或后台，杠杆看 `mt5.account_info().leverage`），也可以我写个探测脚本你跑一次拿全部实测值。

### 14.3 可延后的

| # | 决策 | 建议 |
|---|---|---|
| 10 | `notional=` 是否 v1 支持 | 支持，但回测取价时刻需定义（建议 `ref_price` = 上一根已收盘 bar 的 close） |
| 11 | 止损实现 | 框架内置 stop_engine（跨市场一致），支持原生止损的 venue 可配置委托 |
| 12 | 部分成交后余量处理 | v1 不自动补单，如实回报 `filled_volume`；`set_target` 幂等任务可自动补齐 |
| 13 | FX 点差建模 | 回测撮合必须用 bid/ask 而非 mid，否则收益虚高 |
| 14 | MT5 回报方式 | v1 轮询 200ms（§9.5⑤）；延迟不满足再上 MQL5 EA + socket |
| 15 | 交易量返佣 | IC 三级返佣（每 lot 每边减 $1.50/$2.25/$2.50）v1 按基础费率算，返佣单独记录 |

### 14.4 需 MT5 实测的（不靠推断，不凭记忆写死）

接入 IC Markets 前必须跑一遍并写成断言测试：

| 项 | 取值方式 | 为什么不能推断 |
|---|---|---|
| `native_symbol` 后缀 | `mt5.symbols_get()` | IC 的品种后缀随账户类型/服务器变化（`EURUSDm` / `EURUSD` / 无后缀） |
| `filling_mode` | `symbol_info().filling_mode` 位掩码 | 填错直接 retcode 10030 拒单；不同服务器配置不同 |
| `swap_mode` | `symbol_info().swap_mode` | **决定 swap_long/short 的单位**（points/base ccy/quote ccy/margin ccy/percent），换算错会差数量级 |
| `trade_tick_value` / `trade_tick_size` | `symbol_info()` | pip 价值换算，JPY 对与非 JPY 对不同 |
| `volume_min/max/step` | `symbol_info()` | 最小手数随经纪商配置 |
| `digits` / `point` | `symbol_info()` | 5 位 vs 4 位报价，决定 price_tick 与 pip 关系 |
| 账户模式 | `account_info().trade_mode` | **启动时强校验**，hedging 则拒绝启动（§13.3） |
| 杠杆 | `account_info().leverage` | 监管实体差异（30 vs 500） |
| MT5 retcode 语义 | `mt5.TRADE_RETCODE_*` | 哪些可重试、哪些需对账，决定 `LOCAL_FAILED` 分类 |
| 币安/OKX clientOrderId 限制 | 官方文档 | 字符集与长度，写进 `ids.py` 断言 |

**MT5 无推送通道**这一条已确认（Python 包只有轮询 API），不需再核实。

---

## 15. 借鉴 ccxt：五个可直接落地的模式

**结论：ccxt 作为整体抽象不适合本项目，但五处工程模式比我们当前设计成熟，借鉴成本极低。**

不适合的理由（不复述前文）：不做 position-effect 解析、不支持 MT5、精度走 float、`reduceOnly` 在不同账户类型下类型不一致（字符串 vs 布尔）。

但下面五点直接补上了本文档此前标记为「未解决」或「靠约定」的缺口。

---

### 15.1 能力声明：`CapabilitySet` 替代分散布尔字段 ⛔ 影响 §2.1

**当前设计的问题**：`can_short` / `reduce_only_supported` / `position_mode` 是三个分散的布尔/枚举字段。加一个能力（如冰山单、原生止损）就要改 dataclass + DDL + 所有 loader。而且**看 Spec 定义无法知道"还有哪些能力没建模"**。

**ccxt 的做法**：`exchange.has['createOrder']` / `has['reduceOnly']` —— 单一能力字典。这个模式比分散字段好，但它用字符串键，拼错不报错。

**我们的版本**（枚举 + frozenset）：

```python
"""kq_engine/instruments/capability.py"""
from enum import Enum


class Capability(str, Enum):
    # ── 方向与持仓 ──
    SHORT = "short"                      # 可做空（现货 False）
    HEDGE_MODE = "hedge_mode"            # 双向持仓
    REDUCE_ONLY = "reduce_only"          # 平仓标志（永续 True，FX/现货 False）
    NETTING_AUTO = "netting_auto"        # 经纪商自动净轧（FX netting）

    # ── 订单类型与 TIF ──
    LIMIT = "limit"
    MARKET = "market"
    STOP_MARKET = "stop_market"
    STOP_LIMIT = "stop_limit"
    POST_ONLY = "post_only"
    IOC = "ioc"
    FOK = "fok"
    GTD = "gtd"                          # 带到期时间
    OCO = "oco"
    ICEBERG = "iceberg"
    EDIT_ORDER = "edit_order"            # 改单（不撤重下）
    BATCH_ORDERS = "batch_orders"        # 批量下单
    CANCEL_ALL = "cancel_all"

    # ── 成交与回报 ──
    PARTIAL_FILL = "partial_fill"
    ORDER_STREAM_PUSH = "order_stream_push"      # 有 WS 订单推送；**MT5 = False**
    IDEMPOTENT_CLIENT_ID = "idempotent_client_id" # venue 侧幂等；**MT5 = False**
    TRADE_FEE_IN_RESPONSE = "trade_fee_in_response"  # 回报自带手续费

    # ── 账户 ──
    MARGIN = "margin"
    MULTI_ASSET_MARGIN = "multi_asset_margin"


CapabilitySet = frozenset[Capability]
```

**Spec / VenueDescriptor 改造**：

```python
@dataclass(frozen=True)
class InstrumentSpec:
    ...
    # 删除：can_short / reduce_only_supported
    # 保留：position_mode（它决定 Resolver 走哪个分支，不是能力而是模式）
    capabilities: CapabilitySet        # ← 新增，替代分散布尔
```

DDL 侧不新增列，而是 `capabilities VARCHAR` 存 JSON 数组（能力集会增长，列式建模会反复 ALTER）：

```sql
    capabilities         VARCHAR    NOT NULL,   -- JSON array of Capability values
```

**Resolver 侧的写法变化**：

```python
# 改前
if not spec.reduce_only_supported:
    return Resolution.reject(...)
leg = build_spec(..., reduce_only=True)

# 改后
if Capability.REDUCE_ONLY in spec.capabilities:
    leg = build_spec(..., reduce_only=True)
else:
    leg = build_spec(..., reduce_only=None)   # FX netting / 现货
```

#### 这个改造最大的收益：回测可以刻意削弱能力集

```python
# 测试用例：策略用了 Signal.stop，但 venue 不支持原生止损
BACKTEST_NO_NATIVE_STOP = replace(spec, capabilities=spec.capabilities - {Capability.STOP_MARKET})
```

→ 断言 Router 自动降级到 `stop_engine` 本地止损，且 EventLog 记录降级事件。

**没有能力集就没法做这类测试**——你只能靠"换一个真实 venue"来验证降级路径，而那是实盘事故。这是借鉴 ccxt 后拿到的最大价值，比省下的字段改动重要得多。

#### FX 先行计划的连带好处

MT5 的两个硬约束（无推送、无幂等键）从"代码里特殊处理"变成"**声明**"：

```python
MT5_CAPS = frozenset({
    Capability.MARKET, Capability.LIMIT, Capability.SHORT,
    Capability.NETTING_AUTO, Capability.PARTIAL_FILL,
    Capability.STOP_MARKET, Capability.IOC, Capability.FOK,
    # 刻意不含 ORDER_STREAM_PUSH 与 IDEMPOTENT_CLIENT_ID
})
```

于是 `venue_lane` 可以统一写：

```python
if Capability.ORDER_STREAM_PUSH not in caps:
    self._start_poller(interval_ms=200)      # MT5 走轮询
else:
    self._start_ws_listener()                # crypto 走推送

if Capability.IDEMPOTENT_CLIENT_ID not in caps:
    await self._dedup.assert_new(client_order_id)   # MT5 本地去重
```

**不需要 `if venue == "icmarkets_mt5"` 这类判断。** 这对"先做 FX、后加 crypto"的顺序尤其关键——加 crypto 时是往能力集里加两个成员，不是改控制流。

---

### 15.2 venue 特定参数：白名单化的 `params` 逃生舱 ⚠️ 影响 §4.1

**当前设计的问题**：`position_side` 被我标为"escape hatch，破坏市场无关性"，但没有给出**通用出口**。每遇到一个 venue 特定参数（币安 `workingType`、MT5 `type_time`）就往 `OrderRequest` 加字段 → 字段爆炸，且大部分市场用不到。

**ccxt 的做法**：`create_order(..., params={'positionSide': 'LONG'})` —— 任意键透传，不进统一 API。这个模式是对的，但 ccxt **不校验**，写错键名静默失效。

**我们的版本**（透传 + 白名单 + 显式标记）：

```python
@dataclass(frozen=True)
class OrderRequest:
    ...
    reduce_only: bool = False                        # ← 保留为一等字段（见下）
    venue_params: dict[str, Any] = field(default_factory=dict)   # ← 新增
```

**三个原字段的归属，并非一刀切**：

| 字段 | 归属 | 理由 |
|---|---|---|
| `position_side` | → `venue_params["positionSide"]` | 纯 venue 概念（币安 hedge mode 独有），中性层无从解释 |
| `position_id` | → `venue_params`（或 hedging 专用请求类型） | MT5 hedging 的 ticket，仅该 venue 存在 |
| **`reduce_only`** | **保留一等字段** | **语义市场中性**：表达"只平不开"。Resolver 的平仓腿本身就产出 `reduce_only=True`（§8.1），策略显式传它是在表达中性意图，不是 venue 细节 |

> 判断标准：**该参数能否用市场中性语言描述？**
> 「只减仓不反手」可以 → 一等字段。
> 「作用边是 LONG 还是 SHORT」在 netting 市场无意义 → venue_params。
>
> 把 `reduce_only` 降级成透传参数是错的——那会让「只平不开」这个跨市场通用的意图变成不可移植的写法。

```python
"""kq_engine/venues/param_whitelist.py"""

# 每个 venue 允许透传的键 → 期望类型
VENUE_PARAM_WHITELIST: dict[str, dict[str, type]] = {
    "binance_futures": {
        "positionSide": str,        # 双向持仓模式下的作用边
        "workingType": str,         # MARK_PRICE / CONTRACT_PRICE
        "newOrderRespType": str,    # ACK / RESULT / FULL
        "priceProtect": bool,
    },
    "icmarkets_mt5": {
        "type_time": int,           # ORDER_TIME_SPECIFIED / DAY / ...
        "expiration": int,
        "comment": str,
        "position": int,            # hedging 账户平指定持仓 ticket（§9.4 参考实现）
    },
    "backtest": {},                 # 回测不接受任何透传参数
}


def validate_venue_params(venue: str, params: dict) -> Optional[Rejection]:
    allowed = VENUE_PARAM_WHITELIST.get(venue, {})
    for k, v in params.items():
        if k not in allowed:
            return Rejection(
                RejectionCode.UNKNOWN_VENUE_PARAM,
                f"{venue} 不支持透传参数 {k!r}；允许：{sorted(allowed) or '无'}",
            )
        if not isinstance(v, allowed[k]):
            return Rejection(
                RejectionCode.INVALID_VENUE_PARAM,
                f"参数 {k!r} 类型应为 {allowed[k].__name__}，实际 {type(v).__name__}",
            )
    return None
```

**三条比 ccxt 更严的规则**：

1. **未在白名单 → 拒单，不静默忽略**。ccxt 静默忽略会让策略以为参数生效了（比如 `positionSide` 拼成 `position_side`，结果在 hedge 模式下开错边）。
2. **`venue_params` 非空必须写入 EventLog**，并标记该订单为「不可移植」。回测/实盘一致性测试（§12.2 C1）据此跳过或告警。
3. **回测 venue 白名单为空** → 用了 venue 特定参数的策略在回测阶段就被拒，不会等到实盘才发现回测没覆盖。

**策略侧的写法**：

```python
# 可移植路径（默认）
await api.send_order("BTC/USDT", Decimal("-1"))

# 不可移植路径（显式、有痕迹）
await api.send_order(
    "BTC/USDT", Decimal("-1"),
    venue_params={"positionSide": "LONG"},   # ← 明确知道自己在做什么
)
```

这样"锁仓/双向持仓"这类需求有了正规出口，而**可移植性变成了可度量、可告警的属性**，不是靠文档口头约束。

---

### 15.3 错误码映射：声明式映射表 ⛔ 影响 §11

**当前设计的问题**：§11 定义了 `RejectionCode`，但**没有定义 venue 原生错误码怎么映射过来**。这块如果散在各 adapter 的 `try/except` 里，加一个 venue 就要重写一遍，且极易漏掉「需对账」那一类。

**ccxt 的做法**：`describe()['exceptions']['exact']` —— 原生码到异常类的声明式映射表。这个模式很成熟，直接借鉴。

**我们的版本**（映射到 `RejectionCode` + 三类处置标记）：

```python
"""kq_engine/venues/error_map.py"""
from dataclasses import dataclass, field


@dataclass(frozen=True)
class VenueErrorMap:
    venue: str
    exact: dict[str, RejectionCode] = field(default_factory=dict)
    prefix: dict[str, RejectionCode] = field(default_factory=dict)   # 按前缀/区间匹配
    retryable: frozenset[RejectionCode] = frozenset()
    needs_reconcile: frozenset[str] = frozenset()   # ← 原生码，不是统一码
```

**`needs_reconcile` 是整个映射表里最重要的一类**——它对应 §11 的 `LOCAL_FAILED`：venue 侧状态未知，**不能重试、不能判失败，必须先用 `client_order_id` 反查**。

ccxt 把这类归入 `NetworkError` 但**不区分「交易所可能已收到」**，这正是我们前面几轮反复强调的实盘安全隐患。借鉴它的映射表结构，但必须加这一维。

```python
BINANCE_FUTURES_ERRORS = VenueErrorMap(
    venue="binance_futures",
    exact={
        "-1013": RejectionCode.BELOW_MIN_QTY,        # Filter failure
        "-2010": RejectionCode.INSUFFICIENT_MARGIN,
        "-2011": RejectionCode.UNKNOWN_VENUE_PARAM,  # Cancel rejected
        "-2013": RejectionCode.ORDER_NOT_FOUND,
        "-2019": RejectionCode.INSUFFICIENT_MARGIN,  # Margin is insufficient
        "-4164": RejectionCode.BELOW_MIN_NOTIONAL,   # min notional
        "-4028": RejectionCode.INVALID_VENUE_PARAM,  # invalid leverage
    },
    retryable=frozenset({
        RejectionCode.INSUFFICIENT_AVAILABLE,
        RejectionCode.BELOW_MIN_NOTIONAL,
    }),
    needs_reconcile=frozenset({
        "TIMEOUT", "ECONNRESET", "-1021",            # 时间戳超窗，请求可能已到达
    }),
)

MT5_ERRORS = VenueErrorMap(
    venue="icmarkets_mt5",
    exact={
        # ⚠️ 下表为示意，接入时必须用 mt5.TRADE_RETCODE_* 常量核实后填入，
        #    不得凭记忆硬编码数值（见 §14.4）
        "RETCODE_INVALID_VOLUME":  RejectionCode.BELOW_MIN_QTY,
        "RETCODE_INVALID_PRICE":   RejectionCode.PRICE_TICK_VIOLATION,
        "RETCODE_INVALID_STOPS":   RejectionCode.PRICE_TICK_VIOLATION,
        "RETCODE_INVALID_FILLING": RejectionCode.INVALID_VENUE_PARAM,
        "RETCODE_NO_MONEY":        RejectionCode.INSUFFICIENT_MARGIN,
        "RETCODE_TRADE_DISABLED":  RejectionCode.INSTRUMENT_HALTED,
        "RETCODE_POSITION_CLOSED": RejectionCode.ORDER_NOT_FOUND,   # 已平，幂等安全
        "RETCODE_REQUOTE":         RejectionCode.PRICE_LIMIT_VIOLATION,
    },
    retryable=frozenset({
        RejectionCode.PRICE_LIMIT_VIOLATION,   # requote 可重试（重新取价）
        RejectionCode.INSUFFICIENT_MARGIN,
    }),
    needs_reconcile=frozenset({
        "RETCODE_TIMEOUT",          # ← 最危险：可能已成交
        "RETCODE_CONNECTION",
        "RETCODE_NO_CHANGES",       # 需确认是否已有等价委托
    }),
)
```

**加载器写法**（用常量名而非数值，避免记忆偏差）：

```python
def _build_mt5_map() -> VenueErrorMap:
    import MetaTrader5 as mt5
    return VenueErrorMap(
        venue="icmarkets_mt5",
        exact={
            str(mt5.TRADE_RETCODE_INVALID_VOLUME): RejectionCode.BELOW_MIN_QTY,
            str(mt5.TRADE_RETCODE_NO_MONEY):       RejectionCode.INSUFFICIENT_MARGIN,
            # ... 用常量名，不用 10014 / 10019 这类魔数
        },
        needs_reconcile=frozenset({str(mt5.TRADE_RETCODE_TIMEOUT), ...}),
    )
```

**未映射的错误码怎么办**：默认归 `VENUE_REJECTED` + **写 warning 到 EventLog**，并把原生码与消息完整保留。积累一段时间后回看日志补映射表——这比一开始就想穷举现实得多。

---

### 15.4 sandbox / testnet 模式 ⚠️ 影响 §13.3

**当前设计的问题**：有 `PaperVenue`（本地模拟撮合），但**没有 testnet 概念**——即"连真实 venue 的模拟环境"。这两者不同：testnet 走真实网络协议、真实错误码、真实延迟，只有资金是假的。

现有代码已经有 testnet 配置，但**硬编码在脚本里**：[run_paper.py](<F:\KlineQuantWork\klinequant\scripts\run_paper.py>):39-40 的 `demo-api.binance.com` / `demo-stream.binance.com`。应该提到 venue 元数据里。

**ccxt 的做法**：`exchange.set_sandbox_mode(True)` 切换 URL。借鉴其结构，但加上**能力集可能不同**这一点（ccxt 不处理）：

```python
@dataclass(frozen=True)
class VenueDescriptor:
    venue_key: str
    product: ProductType
    rest_base: str
    ws_base: Optional[str]
    capabilities: CapabilitySet
    error_map: VenueErrorMap

    # ── sandbox ──
    sandbox: Optional["SandboxConfig"] = None


@dataclass(frozen=True)
class SandboxConfig:
    rest_base: str
    ws_base: Optional[str] = None
    capabilities_override: Optional[CapabilitySet] = None   # testnet 常缺功能
    note: str = ""


BINANCE_FUTURES = VenueDescriptor(
    venue_key="binance_futures",
    product=ProductType.CRYPTO_PERP_LINEAR,
    rest_base="https://fapi.binance.com",
    ws_base="wss://fstream.binance.com",
    capabilities=..., error_map=BINANCE_FUTURES_ERRORS,
    sandbox=SandboxConfig(
        rest_base="https://testnet.binancefuture.com",
        ws_base="wss://stream.binancefuture.com",
        # testnet 撮合深度极浅，部分订单类型不支持
        capabilities_override=...,   # 通常比生产少几项
        note="testnet 深度不足，滑点与实盘不可比，仅用于链路验证",
    ),
)
```

**为什么 `capabilities_override` 重要**：币安 testnet 的撮合很薄，且历史上多次不支持某些订单类型。如果按生产能力集下单，会收到一堆"实盘不会出现"的拒单，浪费时间排查。声明 override 后，Router 在 sandbox 模式下会**提前拒掉 venue 根本不支持的单**，给出准确原因。

**三种执行环境的定位**（明确区分，避免混用）：

| 环境 | Venue | 资金 | 撮合 | 用途 |
|---|---|---|---|---|
| 回测 | `BacktestVenue` | 虚拟 | 本地模型 | 策略逻辑与风控验证 |
| 模拟盘 | `PaperVenue` | 虚拟 | 本地模型 + 实时行情 | 长时间稳定性观察 |
| **testnet** | 真实 venue + sandbox 配置 | **假钱真链路** | venue 撮合 | **接入联调、错误码验证** |
| 实盘 | 真实 venue | 真钱 | venue 撮合 | — |

testnet 这一档是当前设计缺失的，而它对 FX 先行计划特别有用：**IC Markets 提供 demo 账户**，可以在不冒资金风险的情况下验证 filling_mode、retcode 映射、轮询节奏这些 §9.5 里最难的点。

---

### 15.5 量化单一入口 + 显式 rounding mode ✅ 已落地于 §6.1

> **代码定义见 §6.1（唯一权威），本节只说明借鉴理由与落地方式。**

**原设计的问题**：四个函数 `quantize_qty` / `quantize_qty_up` / `quantize_price` / `quantize_price_nearest` 并存，调用方要记住"该用哪个"，选错不报错。本文档早期版本里就出现过实际错误——`build_spec` 用 `quantize_qty_up` 处理**价格**，函数名与用途不符。这类 bug 在单一入口下不可能发生。

**ccxt 的做法**：单一 `decimal_to_precision(value, precision, mode)`，mode 显式传。更可审计，直接借鉴（§6.1 已改为 `quantize(value, step, *, mode)`）。

**STRICT 模式的落地方式**：由 Router 注入，Resolver 与策略都不感知。

```python
# 回测环境：strict，任何精度问题立刻抛 QuantityError → 转拒单
router.submit(req, rounding=RoundingMode.STRICT)

# 实盘环境：DOWN，保守量化后如实回报 requested vs filled
router.submit(req, rounding=RoundingMode.DOWN)
```

这实现了前几轮提到的"回测 strict 开关"，且**不需要额外配置项**——它就是 `RoundingMode` 的一个枚举值。传递路径：`Router → validate_and_quantize(..., rounding=...)`（§6.3）。

#### Decimal 边界格式（ccxt 的反面教材）

ccxt 的 `decimal_to_precision` **返回字符串，内部用 float**，所以 `0.1 + 0.2` 这类问题在极端精度下会渗出来。我们的规则更严：

| 位置 | 类型 | 说明 |
|---|---|---|
| 策略 → SDK | `Decimal` | `send_order(sym, Decimal("1.5"))` |
| SDK → 引擎（wire） | **字符串** | `str(Decimal)`，msgpack 传输 |
| 引擎内部 | `Decimal` | 全链路，`localcontext(prec=60)` |
| 引擎 → venue | 字符串或 Decimal | 按 venue 要求（币安 REST 要字符串，MT5 要 float） |
| venue → 引擎 | 原生类型 → `Decimal(str(x))` | **必须经 `str()`**，不能 `Decimal(float)` |
| 引擎 → 策略 | `Decimal` | 代理对象字段 |
| 指标层 | float64 | 唯一允许 float 的区域，边界在 `SeriesHandle.to_numpy()` |

最后一行 venue→引擎 的 `Decimal(str(x))` 是高频错误点：`Decimal(0.1)` 得到 `0.1000000000000000055511151231257827`，而 `Decimal(str(0.1))` 得到 `0.1`。ccxt 内部踩这个坑，我们必须在每个 adapter 的解析函数里守住。

---

### 15.6 明确不借鉴的（避免误抄）

| ccxt 的做法 | 为什么不借鉴 |
|---|---|
| `precisionMode` 全局属性（TICK_SIZE / DECIMAL_PLACES） | 它是**按交易所不同**的，是漏的抽象。我们在 loader 内部消化，Spec 统一存 `qty_step: Decimal`，上层不感知 |
| `has` 用字符串键 | 拼错静默失效。我们用 `Capability` 枚举（§15.1） |
| `params` 无校验透传 | 键名拼错静默失效（`positionSide` vs `position_side`）。我们用白名单 + 类型校验 + 拒单（§15.2） |
| `reduceOnly` 类型不一致（string / bool） | 真实缺陷。我们 `VenueOrderSpec.reduce_only: Optional[bool]` 强类型 |
| 错误分类不含"需对账"维度 | `NetworkError` 不区分 venue 是否已收到。我们加 `needs_reconcile`（§15.3） |
| float 精度计算 | 资金链路不可接受。全 Decimal（§15.5） |
| 无 position-effect 解析 | **这正是本项目 Resolver 层的核心价值**，ccxt 不提供 |
| 统一 API 覆盖 100+ 交易所 | 我们只需 3 个 venue，抽象成本不划算 |

---

### 15.7 对 Week 1 接口层的影响（需现在决定）

这五个模式全部落在 **Week 1 的接口层**，所以必须在写代码前定下来，否则 Week 2 要返工：

| 改动 | 影响文件 | 工作量 |
|---|---|---|
| `Capability` 枚举 + `capabilities` 字段 | `instruments/spec.py`、`capability.py`（新）、DDL 加 1 列 | 小 |
| `venue_params` 承载 `positionSide` / `position`（`reduce_only` 保留为一等字段） | `resolution/base.py`、三个 Resolver | 中（Resolver 分支要改） |
| `VenueErrorMap` + `needs_reconcile` | `venues/error_map.py`（新）、`router.py` | 小 |
| `VenueDescriptor` + `SandboxConfig` | `venues/descriptor.py`（新） | 小 |
| `quantize` 单一入口 + `RoundingMode.STRICT` | `resolution/_quant.py` | 小 |

**合计约 1-2 天额外工作，换取的是加 crypto venue 时不改接口。**

⚠️ 一个需要你判断的取舍：`venue_params` 替代 `position_side` 后，**永续 hedge 模式的锁仓**要走 `venue_params={"positionSide": "LONG"}`。这比原来的显式字段啰嗦，但换来了"可移植性可度量"。如果你认为锁仓是高频需求，可以保留 `position_side` 作为一等字段（放弃这部分可移植性检查）。

---

## 16. 与前几轮设计的衔接

| 前几轮结论 | 本文档对应 |
|---|---|
| `InstrumentRef = (symbol, product)`，策略不感知 venue | §2.1 Spec 的 `symbol` + `product`；`venue`/`native_symbol` 仅内部 |
| 行情绑定即下单路由表 | Registry 按 `(symbol, product)` 查 Spec，Spec 内含 venue |
| Portfolio 在 Venue 之外，回测实盘共用 | §3 PositionState 独立于 Venue；§12.3 测试保障 |
| Order 三态 + terminal_reason | §5.1 `LegIntent` + §11 `RejectionCode`；替换现有 9 态 |
| Executor 只返回 patch，不构造新对象 | §13.1 executors 改造项 |
| client_order_id 网络请求前确定 | §13.2；§11 LOCAL_FAILED 对账依赖它 |
| 精度对齐如实回报 requested vs filled | §10.1 算例；C5 测试 |
| Spec 时间快照（交易所会改精度） | §2.4 DDL 的 effective_from/to |
| Spec 字段来源（API 可得 vs 需推导） | §2.3；`pip_size` 推导、fee 单独接口 |
| Decimal 全链路，指标层才转 float | §2.4 DECIMAL(38,18)；§15.5 边界格式表 |
| MT5 无推送、无幂等键 | §9.5；**§15.1 升级为能力声明**（`ORDER_STREAM_PUSH` / `IDEMPOTENT_CLIENT_ID`） |
| `position_side` 是 escape hatch（未给出通用出口） | **§15.2 已解决**：`venue_params` 白名单 + 可移植性告警 |
| `RejectionCode` 已定但缺 venue 错误码映射机制 | **§15.3 已解决**：`VenueErrorMap` + `needs_reconcile` |
| 缺 testnet 档（只有回测/模拟/实盘） | **§15.4 已解决**：`SandboxConfig` + 能力集 override |
| 回测 strict 量化开关 | **§15.5 已解决**：`RoundingMode.STRICT` |
| 能力声明分散为多个布尔字段 | **§15.1 已解决**：`CapabilitySet`，回测可削弱能力集验证降级路径 |

### 16.1 因本轮借鉴产生的待决项

| # | 待决 | 影响 |
|---|---|---|
| A | 锁仓是否保留 `position_side` 一等字段 | 保留 = 写起来顺但放弃可移植性检查；走 `venue_params` = 啰嗦但可度量（§15.7） |
| B | `Capability` 枚举首版收多少项 | 收太多 = FX 阶段大量恒 False 项；收太少 = 加 crypto 时改枚举。建议按 §15.1 全量收录，FX 只填子集 |
| C | testnet 是否进 Week 1-3 | 建议进：IC demo 账户可提前验证 §9.5 全部难点，不占用资金风险 |
| D | 币安 testnet 的 `capabilities_override` 具体值 | 需实测，不可推断（§14.4 同类） |
